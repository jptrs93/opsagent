// Code generated by cleanproto. DO NOT EDIT.
/**
 * @typedef {Object} TickRequest
 * @property {string} streamId
 * @property {number} count
 * @property {number} intervalMs
 * @property {number} failAfter
 * @property {number} payloadBytes
 */
/**
 * @typedef {Object} Tick
 * @property {string} streamId
 * @property {number} seq
 * @property {Uint8Array} padding
 */
/**
 * @typedef {Object} Chunk
 * @property {string} streamId
 * @property {number} seq
 * @property {Uint8Array} data
 */
/**
 * @typedef {Object} UploadSummary
 * @property {string} streamId
 * @property {number} frames
 * @property {number} totalBytes
 * @property {string} sha256
 */
/**
 * @typedef {Object} EchoRequest
 * @property {string} streamId
 * @property {number} seq
 * @property {string} text
 * @property {boolean} closeStream
 */
/**
 * @typedef {Object} EchoReply
 * @property {string} streamId
 * @property {number} seq
 * @property {string} text
 */
/**
 * @typedef {Object} StreamReportRequest
 * @property {string} streamId
 */
/**
 * @typedef {Object} StreamReport
 * @property {string} streamId
 * @property {string} kind
 * @property {number} messagesIn
 * @property {number} messagesOut
 * @property {boolean} started
 * @property {boolean} done
 * @property {string} result
 */
/**
 * @typedef {Object} AccessPolicy
 * @property {number} policyType
 * @property {string[]} scopes
 */
/**
 * @typedef {Object} ApiErr
 * @property {number} code
 * @property {string} displayErr
 * @property {string} internalErr
 */
import { Reader, Writer } from './runtime.js';

const WIRE = {
    VARINT: 0,
    FIXED64: 1,
    LDELIM: 2,
    FIXED32: 5
};

const tag = (field, wire) => (field << 3) | wire;


/**
 * @param {TickRequest} message
 * @param {Writer} writer
 */
export function writeTickRequest(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.count !== undefined && message.count !== null && message.count !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.count);
    }
    if (message.intervalMs !== undefined && message.intervalMs !== null && message.intervalMs !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.intervalMs);
    }
    if (message.failAfter !== undefined && message.failAfter !== null && message.failAfter !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.failAfter);
    }
    if (message.payloadBytes !== undefined && message.payloadBytes !== null && message.payloadBytes !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int32(message.payloadBytes);
    }
}


/**
 * @param {TickRequest} message
 * @returns {Uint8Array}
 */
export function encodeTickRequest(message) {
    const writer = Writer.create();
    writeTickRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {TickRequest}
 */
function decodeTickRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", count: 0, intervalMs: 0, failAfter: 0, payloadBytes: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.count = reader.int32();
                break;
            }
            case 3: {
                message.intervalMs = reader.int32();
                break;
            }
            case 4: {
                message.failAfter = reader.int32();
                break;
            }
            case 5: {
                message.payloadBytes = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {TickRequest}
 */
export function decodeTickRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeTickRequestMessage(reader);
}



/**
 * @param {Tick} message
 * @param {Writer} writer
 */
export function writeTick(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.seq !== undefined && message.seq !== null && message.seq !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.seq);
    }
    if (message.padding && message.padding.length > 0) {
        writer.uint32(tag(3, WIRE.LDELIM)).bytes(message.padding);
    }
}


/**
 * @param {Tick} message
 * @returns {Uint8Array}
 */
export function encodeTick(message) {
    const writer = Writer.create();
    writeTick(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {Tick}
 */
function decodeTickMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", seq: 0, padding: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.seq = reader.int32();
                break;
            }
            case 3: {
                message.padding = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {Tick}
 */
export function decodeTick(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeTickMessage(reader);
}



/**
 * @param {Chunk} message
 * @param {Writer} writer
 */
export function writeChunk(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.seq !== undefined && message.seq !== null && message.seq !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.seq);
    }
    if (message.data && message.data.length > 0) {
        writer.uint32(tag(3, WIRE.LDELIM)).bytes(message.data);
    }
}


/**
 * @param {Chunk} message
 * @returns {Uint8Array}
 */
export function encodeChunk(message) {
    const writer = Writer.create();
    writeChunk(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {Chunk}
 */
function decodeChunkMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", seq: 0, data: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.seq = reader.int32();
                break;
            }
            case 3: {
                message.data = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {Chunk}
 */
export function decodeChunk(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeChunkMessage(reader);
}



/**
 * @param {UploadSummary} message
 * @param {Writer} writer
 */
export function writeUploadSummary(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.frames !== undefined && message.frames !== null && message.frames !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.frames);
    }
    if (message.totalBytes !== undefined && message.totalBytes !== null && message.totalBytes !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int64(message.totalBytes);
    }
    if (message.sha256 !== undefined && message.sha256 !== null && message.sha256 !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.sha256);
    }
}


/**
 * @param {UploadSummary} message
 * @returns {Uint8Array}
 */
export function encodeUploadSummary(message) {
    const writer = Writer.create();
    writeUploadSummary(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UploadSummary}
 */
function decodeUploadSummaryMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", frames: 0, totalBytes: 0, sha256: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.frames = reader.int32();
                break;
            }
            case 3: {
                message.totalBytes = readInt64(reader, "int64");
                break;
            }
            case 4: {
                message.sha256 = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UploadSummary}
 */
export function decodeUploadSummary(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUploadSummaryMessage(reader);
}



/**
 * @param {EchoRequest} message
 * @param {Writer} writer
 */
export function writeEchoRequest(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.seq !== undefined && message.seq !== null && message.seq !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.seq);
    }
    if (message.text !== undefined && message.text !== null && message.text !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.text);
    }
    if (message.closeStream === true) {
        writer.uint32(tag(4, WIRE.VARINT)).bool(message.closeStream);
    }
}


/**
 * @param {EchoRequest} message
 * @returns {Uint8Array}
 */
export function encodeEchoRequest(message) {
    const writer = Writer.create();
    writeEchoRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EchoRequest}
 */
function decodeEchoRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", seq: 0, text: "", closeStream: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.seq = reader.int32();
                break;
            }
            case 3: {
                message.text = reader.string();
                break;
            }
            case 4: {
                message.closeStream = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EchoRequest}
 */
export function decodeEchoRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEchoRequestMessage(reader);
}



/**
 * @param {EchoReply} message
 * @param {Writer} writer
 */
export function writeEchoReply(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.seq !== undefined && message.seq !== null && message.seq !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.seq);
    }
    if (message.text !== undefined && message.text !== null && message.text !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.text);
    }
}


/**
 * @param {EchoReply} message
 * @returns {Uint8Array}
 */
export function encodeEchoReply(message) {
    const writer = Writer.create();
    writeEchoReply(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EchoReply}
 */
function decodeEchoReplyMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", seq: 0, text: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.seq = reader.int32();
                break;
            }
            case 3: {
                message.text = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EchoReply}
 */
export function decodeEchoReply(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEchoReplyMessage(reader);
}



/**
 * @param {StreamReportRequest} message
 * @param {Writer} writer
 */
export function writeStreamReportRequest(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
}


/**
 * @param {StreamReportRequest} message
 * @returns {Uint8Array}
 */
export function encodeStreamReportRequest(message) {
    const writer = Writer.create();
    writeStreamReportRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {StreamReportRequest}
 */
function decodeStreamReportRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {StreamReportRequest}
 */
export function decodeStreamReportRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeStreamReportRequestMessage(reader);
}



/**
 * @param {StreamReport} message
 * @param {Writer} writer
 */
export function writeStreamReport(message, writer) {
    if (message.streamId !== undefined && message.streamId !== null && message.streamId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.streamId);
    }
    if (message.kind !== undefined && message.kind !== null && message.kind !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.kind);
    }
    if (message.messagesIn !== undefined && message.messagesIn !== null && message.messagesIn !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.messagesIn);
    }
    if (message.messagesOut !== undefined && message.messagesOut !== null && message.messagesOut !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.messagesOut);
    }
    if (message.started === true) {
        writer.uint32(tag(5, WIRE.VARINT)).bool(message.started);
    }
    if (message.done === true) {
        writer.uint32(tag(6, WIRE.VARINT)).bool(message.done);
    }
    if (message.result !== undefined && message.result !== null && message.result !== "") {
        writer.uint32(tag(7, WIRE.LDELIM)).string(message.result);
    }
}


/**
 * @param {StreamReport} message
 * @returns {Uint8Array}
 */
export function encodeStreamReport(message) {
    const writer = Writer.create();
    writeStreamReport(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {StreamReport}
 */
function decodeStreamReportMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {streamId: "", kind: "", messagesIn: 0, messagesOut: 0, started: false, done: false, result: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.streamId = reader.string();
                break;
            }
            case 2: {
                message.kind = reader.string();
                break;
            }
            case 3: {
                message.messagesIn = reader.int32();
                break;
            }
            case 4: {
                message.messagesOut = reader.int32();
                break;
            }
            case 5: {
                message.started = reader.bool();
                break;
            }
            case 6: {
                message.done = reader.bool();
                break;
            }
            case 7: {
                message.result = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {StreamReport}
 */
export function decodeStreamReport(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeStreamReportMessage(reader);
}



/**
 * @param {AccessPolicy} message
 * @param {Writer} writer
 */
export function writeAccessPolicy(message, writer) {
    if (message.policyType !== undefined && message.policyType !== null && message.policyType !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.policyType);
    }
    if (message.scopes && message.scopes.length > 0) {
        for (const item of message.scopes) {
            writer.uint32(tag(2, WIRE.LDELIM)).string(item);
        }
    }
}


/**
 * @param {AccessPolicy} message
 * @returns {Uint8Array}
 */
export function encodeAccessPolicy(message) {
    const writer = Writer.create();
    writeAccessPolicy(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AccessPolicy}
 */
function decodeAccessPolicyMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {policyType: 0, scopes: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.policyType = reader.int32();
                break;
            }
            case 2: {
                message.scopes.push(reader.string());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AccessPolicy}
 */
export function decodeAccessPolicy(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAccessPolicyMessage(reader);
}



/**
 * @param {ApiErr} message
 * @param {Writer} writer
 */
export function writeApiErr(message, writer) {
    if (message.code !== undefined && message.code !== null && message.code !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.code);
    }
    if (message.displayErr !== undefined && message.displayErr !== null && message.displayErr !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.displayErr);
    }
    if (message.internalErr !== undefined && message.internalErr !== null && message.internalErr !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.internalErr);
    }
}


/**
 * @param {ApiErr} message
 * @returns {Uint8Array}
 */
export function encodeApiErr(message) {
    const writer = Writer.create();
    writeApiErr(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ApiErr}
 */
function decodeApiErrMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {code: 0, displayErr: "", internalErr: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.code = reader.int32();
                break;
            }
            case 2: {
                message.displayErr = reader.string();
                break;
            }
            case 3: {
                message.internalErr = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ApiErr}
 */
export function decodeApiErr(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeApiErrMessage(reader);
}



function readInt64(reader, method) {
    const value = reader[method]();
    if (typeof value === "number") {
        return value;
    }
    if (typeof value === "bigint") {
        return Number(value);
    }
    return value.toNumber();
}
