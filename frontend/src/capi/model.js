// Code generated by cleanproto. DO NOT EDIT.
/**
 * @typedef {Object} EnrollmentWorkerMsg
 * @property {EnrollmentHello} hello
 */
/**
 * @typedef {Object} EnrollmentHello
 * @property {string} requestingMachineId
 * @property {Uint8Array} workerCertificateRequest
 * @property {string} opendeployVersion
 */
/**
 * @typedef {Object} EnrollmentPrimaryMsg
 * @property {EnrollmentRequestStatus} requestStatus
 * @property {EnrollmentAccepted} accepted
 */
/**
 * @typedef {Object} EnrollmentRequestStatus
 * @property {number} id
 * @property {Date} createdAt
 * @property {Date} updatedAt
 * @property {string} requestingIpAddress
 * @property {string} requestingMachineId
 * @property {string} status
 * @property {string} opendeployVersion
 */
/**
 * @typedef {Object} EnrollmentRequestList
 * @property {EnrollmentRequestStatus[]} items
 */
/**
 * @typedef {Object} EnrollmentAcceptRequest
 * @property {number} id
 * @property {string} workerName
 */
/**
 * @typedef {Object} EnrollmentAccepted
 * @property {number} id
 * @property {string} workerName
 * @property {Uint8Array} caCertificate
 * @property {Uint8Array} workerCertificate
 */
/**
 * @typedef {Object} GithubCredentials
 * @property {string} token
 * @property {Date} changedAt
 */
/**
 * @typedef {Object} ClusterAssetRequest
 * @property {number} assetId
 * @property {number} version
 */
/**
 * @typedef {Object} ClusterAssetBlob
 * @property {number} assetId
 * @property {string} key
 * @property {number} version
 * @property {string} format
 * @property {Uint8Array} blob
 */
/**
 * @typedef {Object} ClusterSecretsRequest
 * @property {number[]} ids
 */
/**
 * @typedef {Object} ClusterSecretValue
 * @property {number} id
 * @property {Uint8Array} value
 */
/**
 * @typedef {Object} ClusterSecretsResponse
 * @property {ClusterSecretValue[]} items
 */
/**
 * @typedef {Object} ClusterConfigsRequest
 * @property {number[]} ids
 */
/**
 * @typedef {Object} ClusterConfigValue
 * @property {number} id
 * @property {string} value
 */
/**
 * @typedef {Object} ClusterConfigsResponse
 * @property {ClusterConfigValue[]} items
 */
/**
 * @typedef {Object} SecretValue
 * @property {string} key
 */
/**
 * @typedef {Object} DynamicConfiguration
 * @property {string} webListen
 * @property {boolean} webHttpOnly
 * @property {string} clusterListen
 * @property {string} enrollmentListen
 * @property {string[]} acmeHosts
 * @property {string} acmeEmail
 * @property {SecretValue} githubToken
 * @property {string} backupS3AccessKeyId
 * @property {SecretValue} backupS3SecretAccessKey
 * @property {string} backupS3Bucket
 * @property {string} backupS3Path
 * @property {string} backupS3Region
 * @property {string} backupS3Endpoint
 * @property {boolean} backupEnabled
 */
/**
 * @typedef {Object} ConfigUpdateRequest
 * @property {ConfigValueUpdate[]} values
 */
/**
 * @typedef {Object} ConfigValueUpdate
 * @property {string} key
 * @property {string} value
 */
/**
 * @typedef {Object} EmptyRequest
 */
/**
 * @typedef {Object} MasterPasswordRequest
 * @property {string} password
 * @property {string} username
 */
/**
 * @typedef {Object} MasterPasswordVerifyRequest
 * @property {string} password
 */
/**
 * @typedef {Object} MasterPasswordSaveRequest
 * @property {string} password
 */
/**
 * @typedef {Object} LoginResponse
 * @property {string} token
 * @property {number} userId
 * @property {string[]} scopes
 * @property {string} name
 * @property {Date} expiry
 */
/**
 * @typedef {Object} WebAuthNOptionsResponse
 * @property {string} sessionId
 * @property {Uint8Array} optionsJson
 */
/**
 * @typedef {Object} WebAuthNFinishRequest
 * @property {string} sessionId
 * @property {Uint8Array} credentialJson
 */
/**
 * @typedef {Object} SecretMeta
 * @property {string} name
 * @property {string} group
 * @property {Date} createdAt
 * @property {Date} updatedAt
 * @property {number} updatedBy
 * @property {number} id
 * @property {boolean} deleted
 * @property {number} spaceId
 */
/**
 * @typedef {Object} SecretList
 * @property {SecretMeta[]} items
 */
/**
 * @typedef {Object} SecretSetRequest
 * @property {string} name
 * @property {string} group
 * @property {Uint8Array} value
 * @property {number} spaceId
 */
/**
 * @typedef {Object} SecretDeleteRequest
 * @property {string} name
 */
/**
 * @typedef {Object} SecretRevealRequest
 * @property {string} name
 */
/**
 * @typedef {Object} SecretRevealResponse
 * @property {Uint8Array} value
 */
/**
 * @typedef {Object} SecretsStatusResponse
 * @property {boolean} unlocked
 * @property {boolean} recoveryConfigured
 */
/**
 * @typedef {Object} SecretRecoveryCodeResponse
 * @property {string} code
 */
/**
 * @typedef {Object} SecretUnlockRequest
 * @property {string} code
 */
/**
 * @typedef {Object} UserConfig
 * @property {string} name
 * @property {string} group
 * @property {string} value
 * @property {Date} createdAt
 * @property {Date} updatedAt
 * @property {number} updatedBy
 * @property {number} id
 * @property {boolean} deleted
 * @property {number} spaceId
 */
/**
 * @typedef {Object} UserConfigList
 * @property {UserConfig[]} items
 */
/**
 * @typedef {Object} UserConfigSetRequest
 * @property {string} name
 * @property {string} group
 * @property {string} value
 * @property {number} spaceId
 */
/**
 * @typedef {Object} UserConfigDeleteRequest
 * @property {string} name
 */
/**
 * @typedef {Object} AssetMeta
 * @property {string} key
 * @property {Date} createdAt
 * @property {number} version
 * @property {string} format
 * @property {string} location
 * @property {number} sizeBytes
 * @property {number} id
 * @property {number} spaceId
 */
/**
 * @typedef {Object} Asset
 * @property {string} key
 * @property {Date} createdAt
 * @property {number} version
 * @property {string} format
 * @property {string} location
 * @property {Uint8Array} blob
 * @property {number} id
 * @property {number} spaceId
 */
/**
 * @typedef {Object} AssetList
 * @property {AssetMeta[]} items
 */
/**
 * @typedef {Object} AssetGetRequest
 * @property {string} key
 * @property {number} version
 */
/**
 * @typedef {Object} AssetSetRequest
 * @property {string} key
 * @property {string} format
 * @property {Uint8Array} blob
 * @property {number} spaceId
 */
/**
 * @typedef {Object} AssetDeleteRequest
 * @property {string} key
 */
/**
 * @typedef {Object} NixDockerBuildConfig
 * @property {string} repo
 * @property {string} flake
 */
/**
 * @typedef {Object} GithubReleaseConfig
 * @property {string} repo
 * @property {string} asset
 * @property {string} tag
 * @property {string} downloadScript
 */
/**
 * @typedef {Object} ContainerImageConfig
 * @property {string} image
 */
/**
 * @typedef {Object} PrepareConfig
 * @property {GithubReleaseConfig} githubRelease
 * @property {ContainerImageConfig} containerImage
 * @property {NixDockerBuildConfig} nixDockerBuild
 */
/**
 * @typedef {Object} EnvVarValue
 * @property {number} secretId
 * @property {number} configId
 * @property {string} value
 */
/**
 * @typedef {Object} SystemdRunnerConfig
 * @property {string} name
 * @property {string} binPath
 */
/**
 * @typedef {Object} ContainerMount
 * @property {string} host
 * @property {string} container
 * @property {boolean} readonly
 */
/**
 * @typedef {Object} ContainerAssetMount
 * @property {string} asset
 * @property {number} version
 * @property {string} path
 * @property {string} format
 * @property {number} assetId
 */
/**
 * @typedef {Object} ContainerRunnerConfig
 * @property {string} user
 * @property {Object.<string, EnvVarValue>} envVars
 * @property {string[]} command
 * @property {string} workingDir
 * @property {string} dataMountPath
 * @property {boolean} disableDataVolume
 * @property {ContainerMount[]} mounts
 * @property {ContainerAssetMount[]} assetMounts
 */
/**
 * @typedef {Object} RunnerConfig
 * @property {SystemdRunnerConfig} systemd
 * @property {ContainerRunnerConfig} container
 */
/**
 * @typedef {Object} State
 * @property {boolean} heartbeat
 * @property {DeploymentWithStatusSnapshot} deploymentsSnapshot
 * @property {DeploymentWithStatus} deploymentUpdate
 * @property {User[]} usersSnapshot
 * @property {User} userUpdate
 * @property {ClusterMachineList} machinesSnapshot
 * @property {ClusterMachine} machineUpdate
 * @property {EnrollmentRequestList} enrollmentsSnapshot
 * @property {EnrollmentRequestStatus} enrollmentUpdate
 * @property {SecretReferenceList} secretsSnapshot
 * @property {SecretReference} secretUpdate
 * @property {UserConfigReferenceList} userConfigsSnapshot
 * @property {UserConfigReference} userConfigUpdate
 * @property {SecretsStatusResponse} secretsStatusSnapshot
 * @property {SecretList} secretMetasSnapshot
 * @property {SecretMeta} secretMetaUpdate
 * @property {UserConfigList} userConfigValuesSnapshot
 * @property {UserConfig} userConfigValueUpdate
 * @property {SpaceList} spacesSnapshot
 * @property {Space} spaceUpdate
 */
/**
 * @typedef {Object} Space
 * @property {number} id
 * @property {string} name
 * @property {boolean} deleted
 */
/**
 * @typedef {Object} SpaceList
 * @property {Space[]} items
 */
/**
 * @typedef {Object} SpaceSetRequest
 * @property {number} id
 * @property {string} name
 */
/**
 * @typedef {Object} SpaceDeleteRequest
 * @property {number} id
 */
/**
 * @typedef {Object} SecretReference
 * @property {number} id
 * @property {string} name
 * @property {boolean} deleted
 * @property {number} spaceId
 */
/**
 * @typedef {Object} SecretReferenceList
 * @property {SecretReference[]} items
 */
/**
 * @typedef {Object} UserConfigReference
 * @property {number} id
 * @property {string} name
 * @property {boolean} deleted
 * @property {number} spaceId
 */
/**
 * @typedef {Object} UserConfigReferenceList
 * @property {UserConfigReference[]} items
 */
/**
 * @typedef {Object} DeploymentWithStatusSnapshot
 * @property {DeploymentWithStatus[]} items
 */
/**
 * @typedef {Object} ClusterMachineList
 * @property {ClusterMachine[]} items
 */
/**
 * @typedef {Object} DeploymentHistoryEntry
 * @property {DeploymentConfig} config
 * @property {DeploymentStatus} status
 */
/**
 * @typedef {Object} DeploymentHistory
 * @property {DeploymentHistoryEntry[]} entries
 */
/**
 * @typedef {Object} DeploymentUpdateRequest
 * @property {number} deploymentId
 * @property {string} targetVersion
 * @property {boolean} stop
 * @property {number} version
 * @property {DeploymentSpec} spec
 * @property {number} spaceId
 */
/**
 * @typedef {Object} DeploymentCreateRequest
 * @property {DeploymentIdentifier} configId
 * @property {DeploymentSpec} spec
 */
/**
 * @typedef {Object} DeploymentHistoryRequest
 * @property {number} deploymentId
 */
/**
 * @typedef {Object} PrepareOutputRequest
 * @property {number} deploymentId
 * @property {number} version
 */
/**
 * @typedef {Object} PrepareOutputChunk
 * @property {Uint8Array} data
 */
/**
 * @typedef {Object} RunOutputRequest
 * @property {number} deploymentId
 * @property {number} version
 */
/**
 * @typedef {Object} DeploymentLogRequest
 * @property {RunOutputRequest} runnerOutput
 * @property {PrepareOutputRequest} preparerOutput
 * @property {string} requestId
 */
/**
 * @typedef {Object} LogSearchRequest
 * @property {number} deploymentId
 * @property {Date} timeStart
 * @property {Date} timeEnd
 * @property {string} levelMin
 * @property {Object.<string, string>} searchKeys
 * @property {string} requestId
 * @property {number} logLineLimit
 */
/**
 * @typedef {Object} LogLine
 * @property {Date} time
 * @property {string} level
 * @property {string} msg
 * @property {Object.<string, string>} props
 */
/**
 * @typedef {Object} LogLineBatch
 * @property {LogLine[]} lines
 */
/**
 * @typedef {Object} DeploymentIdentifier
 * @property {number} spaceId
 * @property {string} machine
 * @property {string} name
 */
/**
 * @typedef {Object} DeploymentSpec
 * @property {PrepareConfig} prepare
 * @property {RunnerConfig} runner
 */
/**
 * @typedef {Object} DesiredState
 * @property {string} version
 * @property {boolean} running
 */
/**
 * @typedef {Object} DeploymentConfig
 * @property {number} id
 * @property {DeploymentIdentifier} configId
 * @property {number} version
 * @property {Date} updatedAt
 * @property {number} updatedBy
 * @property {DeploymentSpec} spec
 * @property {DesiredState} desiredState
 * @property {boolean} deleted
 * @property {Date} createdAt
 */
/**
 * @typedef {Object} DeploymentWithStatus
 * @property {DeploymentConfig} config
 * @property {DeploymentStatus} status
 */
/**
 * @typedef {Object} DeploymentStatus
 * @property {Date} updatedAt
 * @property {number} deploymentId
 * @property {PreparerStatus} preparer
 * @property {RunnerStatus} runner
 */
/**
 * @typedef {Object} PreparerStatus
 * @property {number} deploymentConfigVersion
 * @property {string} artifact
 * @property {number} status
 */
/**
 * @typedef {Object} RunnerStatus
 * @property {number} deploymentConfigVersion
 * @property {number} runningPid
 * @property {string} runningArtifact
 * @property {number} status
 * @property {number} numberOfRestarts
 * @property {Date} lastRestartAt
 * @property {string} runningVersion
 */
/**
 * @typedef {Object} Version
 * @property {string} id
 * @property {string} label
 * @property {string} author
 * @property {Date} time
 */
/**
 * @typedef {Object} DeploymentVersions
 * @property {number} deploymentId
 * @property {DeploymentNixDockerBuildVersions} nixDockerBuild
 * @property {DeploymentGithubReleaseVersions} githubRelease
 * @property {DeploymentContainerImageVersions} containerImage
 */
/**
 * @typedef {Object} DeploymentNixDockerBuildVersions
 * @property {string[]} branches
 * @property {string} selectedBranch
 * @property {Version[]} commits
 */
/**
 * @typedef {Object} DeploymentGithubReleaseVersions
 * @property {Version[]} releases
 */
/**
 * @typedef {Object} DeploymentContainerImageVersions
 * @property {Version[]} tags
 */
/**
 * @typedef {Object} DeploymentVersionsRequest
 * @property {number} deploymentId
 * @property {string} selectedBranch
 */
/**
 * @typedef {Object} ValidateSourceRequest
 * @property {ValidateNixDockerBuildSource} nixDockerBuild
 * @property {ValidateContainerImageSource} containerImage
 */
/**
 * @typedef {Object} ValidateNixDockerBuildSource
 * @property {string} repoUrl
 * @property {string} selectedBranch
 * @property {Version} selectedCommit
 * @property {string} selectedFlakePath
 * @property {boolean} refreshAvailableBranches
 * @property {boolean} refreshAvailableCommits
 * @property {boolean} checkRepo
 * @property {boolean} checkBranch
 * @property {boolean} checkCommit
 * @property {boolean} checkFlakePath
 */
/**
 * @typedef {Object} ValidateNixDockerBuildSourceResponse
 * @property {string} checkedRepoUrl
 * @property {ValidationResult} gitRepository
 * @property {string} checkedBranch
 * @property {ValidationResult} branchCheck
 * @property {Version} checkedCommit
 * @property {ValidationResult} commitCheck
 * @property {string} checkedFlakePath
 * @property {ValidationResult} nixFlakeFile
 * @property {AvailableBranches} availableBranches
 * @property {AvailableCommits} availableCommits
 */
/**
 * @typedef {Object} AvailableCommits
 * @property {boolean} loaded
 * @property {string} branch
 * @property {string} errormessage
 * @property {Version[]} commits
 */
/**
 * @typedef {Object} AvailableBranches
 * @property {boolean} loaded
 * @property {string} errormessage
 * @property {string[]} branches
 */
/**
 * @typedef {Object} RepoValidationResult
 * @property {boolean} checked
 * @property {string} repoUrl
 * @property {boolean} ok
 * @property {string} message
 */
/**
 * @typedef {Object} ValidationResult
 * @property {boolean} checked
 * @property {boolean} ok
 * @property {string} message
 */
/**
 * @typedef {Object} ValidateContainerImageSource
 * @property {string} image
 * @property {boolean} refreshVersions
 */
/**
 * @typedef {Object} ValidateSourceResponse
 * @property {ValidateNixDockerBuildSourceResponse} nixDockerBuild
 * @property {ValidateContainerImageSourceResponse} containerImage
 */
/**
 * @typedef {Object} ValidateContainerImageSourceResponse
 * @property {ValidationResult} image
 * @property {Version[]} tags
 */
/**
 * @typedef {Object} User
 * @property {number} id
 * @property {string} name
 */
/**
 * @typedef {Object} WebAuthnCredential
 * @property {Uint8Array} id
 * @property {Uint8Array} data
 */
/**
 * @typedef {Object} InternalUser
 * @property {number} id
 * @property {Uint8Array} webAuthNId
 * @property {string} name
 * @property {WebAuthnCredential[]} credentials
 */
/**
 * @typedef {Object} PublicKeyRecord
 * @property {string} kid
 * @property {Uint8Array} keyBytes
 */
/**
 * @typedef {Object} ClusterMachine
 * @property {string} name
 * @property {boolean} isPrimary
 * @property {boolean} connected
 * @property {Date} connectedAt
 */
/**
 * @typedef {Object} ClusterStatusResponse
 * @property {ClusterMachine[]} machines
 */
/**
 * @typedef {Object} MsgToWorker
 * @property {DeploymentWithStatusSnapshot} deploymentsSnapshot
 * @property {DeploymentConfig} deploymentUpdate
 * @property {PrepareOutputRequest} prepareLogRequest
 * @property {RunOutputRequest} runLogRequest
 * @property {DeploymentLogRequest} deploymentLogRequest
 * @property {string} stopLogRequestId
 * @property {LogSearchRequest} logSearchRequest
 */
/**
 * @typedef {Object} MsgToMaster
 * @property {DeploymentStatus} statusWrite
 * @property {Uint8Array} logData
 * @property {boolean} logEnd
 * @property {string} logRequestId
 * @property {LogLineBatch} logLines
 */
/**
 * @typedef {Object} AccessPolicy
 * @property {number} policyType
 * @property {string[]} scopes
 */
/**
 * @typedef {Object} ApiErr
 * @property {number} code
 * @property {string} displayErr
 * @property {string} internalErr
 */
import { Reader, Writer } from './runtime.js';

const WIRE = {
    VARINT: 0,
    FIXED64: 1,
    LDELIM: 2,
    FIXED32: 5
};

const tag = (field, wire) => (field << 3) | wire;


/**
 * @param {EnrollmentWorkerMsg} message
 * @param {Writer} writer
 */
export function writeEnrollmentWorkerMsg(message, writer) {
    if (message.hello !== undefined && message.hello !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeEnrollmentHello(message.hello, writer);
        writer.ldelim();
    }
}


/**
 * @param {EnrollmentWorkerMsg} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentWorkerMsg(message) {
    const writer = Writer.create();
    writeEnrollmentWorkerMsg(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentWorkerMsg}
 */
function decodeEnrollmentWorkerMsgMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {hello: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.hello = decodeEnrollmentHelloMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentWorkerMsg}
 */
export function decodeEnrollmentWorkerMsg(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentWorkerMsgMessage(reader);
}



/**
 * @param {EnrollmentHello} message
 * @param {Writer} writer
 */
export function writeEnrollmentHello(message, writer) {
    if (message.requestingMachineId !== undefined && message.requestingMachineId !== null && message.requestingMachineId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.requestingMachineId);
    }
    if (message.workerCertificateRequest && message.workerCertificateRequest.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.workerCertificateRequest);
    }
    if (message.opendeployVersion !== undefined && message.opendeployVersion !== null && message.opendeployVersion !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.opendeployVersion);
    }
}


/**
 * @param {EnrollmentHello} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentHello(message) {
    const writer = Writer.create();
    writeEnrollmentHello(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentHello}
 */
function decodeEnrollmentHelloMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {requestingMachineId: "", workerCertificateRequest: new Uint8Array(0), opendeployVersion: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.requestingMachineId = reader.string();
                break;
            }
            case 2: {
                message.workerCertificateRequest = reader.bytes();
                break;
            }
            case 3: {
                message.opendeployVersion = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentHello}
 */
export function decodeEnrollmentHello(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentHelloMessage(reader);
}



/**
 * @param {EnrollmentPrimaryMsg} message
 * @param {Writer} writer
 */
export function writeEnrollmentPrimaryMsg(message, writer) {
    if (message.requestStatus !== undefined && message.requestStatus !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeEnrollmentRequestStatus(message.requestStatus, writer);
        writer.ldelim();
    }
    if (message.accepted !== undefined && message.accepted !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeEnrollmentAccepted(message.accepted, writer);
        writer.ldelim();
    }
}


/**
 * @param {EnrollmentPrimaryMsg} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentPrimaryMsg(message) {
    const writer = Writer.create();
    writeEnrollmentPrimaryMsg(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentPrimaryMsg}
 */
function decodeEnrollmentPrimaryMsgMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {requestStatus: undefined, accepted: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.requestStatus = decodeEnrollmentRequestStatusMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.accepted = decodeEnrollmentAcceptedMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentPrimaryMsg}
 */
export function decodeEnrollmentPrimaryMsg(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentPrimaryMsgMessage(reader);
}



/**
 * @param {EnrollmentRequestStatus} message
 * @param {Writer} writer
 */
export function writeEnrollmentRequestStatus(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
    if (message.updatedAt instanceof Date && message.updatedAt.getTime() !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int64(Math.trunc(message.updatedAt.getTime()));
    }
    if (message.requestingIpAddress !== undefined && message.requestingIpAddress !== null && message.requestingIpAddress !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.requestingIpAddress);
    }
    if (message.requestingMachineId !== undefined && message.requestingMachineId !== null && message.requestingMachineId !== "") {
        writer.uint32(tag(5, WIRE.LDELIM)).string(message.requestingMachineId);
    }
    if (message.status !== undefined && message.status !== null && message.status !== "") {
        writer.uint32(tag(6, WIRE.LDELIM)).string(message.status);
    }
    if (message.opendeployVersion !== undefined && message.opendeployVersion !== null && message.opendeployVersion !== "") {
        writer.uint32(tag(7, WIRE.LDELIM)).string(message.opendeployVersion);
    }
}


/**
 * @param {EnrollmentRequestStatus} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentRequestStatus(message) {
    const writer = Writer.create();
    writeEnrollmentRequestStatus(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentRequestStatus}
 */
function decodeEnrollmentRequestStatusMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, createdAt: new Date(0), updatedAt: new Date(0), requestingIpAddress: "", requestingMachineId: "", status: "", opendeployVersion: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 3: {
                message.updatedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 4: {
                message.requestingIpAddress = reader.string();
                break;
            }
            case 5: {
                message.requestingMachineId = reader.string();
                break;
            }
            case 6: {
                message.status = reader.string();
                break;
            }
            case 7: {
                message.opendeployVersion = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentRequestStatus}
 */
export function decodeEnrollmentRequestStatus(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentRequestStatusMessage(reader);
}



/**
 * @param {EnrollmentRequestList} message
 * @param {Writer} writer
 */
export function writeEnrollmentRequestList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeEnrollmentRequestStatus(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {EnrollmentRequestList} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentRequestList(message) {
    const writer = Writer.create();
    writeEnrollmentRequestList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentRequestList}
 */
function decodeEnrollmentRequestListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeEnrollmentRequestStatusMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentRequestList}
 */
export function decodeEnrollmentRequestList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentRequestListMessage(reader);
}



/**
 * @param {EnrollmentAcceptRequest} message
 * @param {Writer} writer
 */
export function writeEnrollmentAcceptRequest(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.workerName !== undefined && message.workerName !== null && message.workerName !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.workerName);
    }
}


/**
 * @param {EnrollmentAcceptRequest} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentAcceptRequest(message) {
    const writer = Writer.create();
    writeEnrollmentAcceptRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentAcceptRequest}
 */
function decodeEnrollmentAcceptRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, workerName: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.workerName = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentAcceptRequest}
 */
export function decodeEnrollmentAcceptRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentAcceptRequestMessage(reader);
}



/**
 * @param {EnrollmentAccepted} message
 * @param {Writer} writer
 */
export function writeEnrollmentAccepted(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.workerName !== undefined && message.workerName !== null && message.workerName !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.workerName);
    }
    if (message.caCertificate && message.caCertificate.length > 0) {
        writer.uint32(tag(3, WIRE.LDELIM)).bytes(message.caCertificate);
    }
    if (message.workerCertificate && message.workerCertificate.length > 0) {
        writer.uint32(tag(4, WIRE.LDELIM)).bytes(message.workerCertificate);
    }
}


/**
 * @param {EnrollmentAccepted} message
 * @returns {Uint8Array}
 */
export function encodeEnrollmentAccepted(message) {
    const writer = Writer.create();
    writeEnrollmentAccepted(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnrollmentAccepted}
 */
function decodeEnrollmentAcceptedMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, workerName: "", caCertificate: new Uint8Array(0), workerCertificate: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.workerName = reader.string();
                break;
            }
            case 3: {
                message.caCertificate = reader.bytes();
                break;
            }
            case 4: {
                message.workerCertificate = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnrollmentAccepted}
 */
export function decodeEnrollmentAccepted(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnrollmentAcceptedMessage(reader);
}



/**
 * @param {GithubCredentials} message
 * @param {Writer} writer
 */
export function writeGithubCredentials(message, writer) {
    if (message.token !== undefined && message.token !== null && message.token !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.token);
    }
    if (message.changedAt instanceof Date && message.changedAt.getTime() !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int64(Math.trunc(message.changedAt.getTime()));
    }
}


/**
 * @param {GithubCredentials} message
 * @returns {Uint8Array}
 */
export function encodeGithubCredentials(message) {
    const writer = Writer.create();
    writeGithubCredentials(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {GithubCredentials}
 */
function decodeGithubCredentialsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {token: "", changedAt: new Date(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.token = reader.string();
                break;
            }
            case 2: {
                message.changedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {GithubCredentials}
 */
export function decodeGithubCredentials(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeGithubCredentialsMessage(reader);
}



/**
 * @param {ClusterAssetRequest} message
 * @param {Writer} writer
 */
export function writeClusterAssetRequest(message, writer) {
    if (message.assetId !== undefined && message.assetId !== null && message.assetId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.assetId);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
}


/**
 * @param {ClusterAssetRequest} message
 * @returns {Uint8Array}
 */
export function encodeClusterAssetRequest(message) {
    const writer = Writer.create();
    writeClusterAssetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterAssetRequest}
 */
function decodeClusterAssetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {assetId: 0, version: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.assetId = reader.int32();
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterAssetRequest}
 */
export function decodeClusterAssetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterAssetRequestMessage(reader);
}



/**
 * @param {ClusterAssetBlob} message
 * @param {Writer} writer
 */
export function writeClusterAssetBlob(message, writer) {
    if (message.assetId !== undefined && message.assetId !== null && message.assetId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.assetId);
    }
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.key);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.version);
    }
    if (message.format !== undefined && message.format !== null && message.format !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.format);
    }
    if (message.blob && message.blob.length > 0) {
        writer.uint32(tag(5, WIRE.LDELIM)).bytes(message.blob);
    }
}


/**
 * @param {ClusterAssetBlob} message
 * @returns {Uint8Array}
 */
export function encodeClusterAssetBlob(message) {
    const writer = Writer.create();
    writeClusterAssetBlob(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterAssetBlob}
 */
function decodeClusterAssetBlobMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {assetId: 0, key: "", version: 0, format: "", blob: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.assetId = reader.int32();
                break;
            }
            case 2: {
                message.key = reader.string();
                break;
            }
            case 3: {
                message.version = reader.int32();
                break;
            }
            case 4: {
                message.format = reader.string();
                break;
            }
            case 5: {
                message.blob = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterAssetBlob}
 */
export function decodeClusterAssetBlob(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterAssetBlobMessage(reader);
}



/**
 * @param {ClusterSecretsRequest} message
 * @param {Writer} writer
 */
export function writeClusterSecretsRequest(message, writer) {
    if (message.ids) {
        const packedWriter = Writer.create();
        for (const item of message.ids) {
            packedWriter.int32(item);
        }
        if (packedWriter.len > 0) {
            writer.uint32(tag(1, WIRE.LDELIM)).bytes(packedWriter.finish());
        }
    }
}


/**
 * @param {ClusterSecretsRequest} message
 * @returns {Uint8Array}
 */
export function encodeClusterSecretsRequest(message) {
    const writer = Writer.create();
    writeClusterSecretsRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterSecretsRequest}
 */
function decodeClusterSecretsRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {ids: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                const end2 = reader.uint32() + reader.pos;
                while (reader.pos < end2) {
                    message.ids.push(reader.int32());
                }
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterSecretsRequest}
 */
export function decodeClusterSecretsRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterSecretsRequestMessage(reader);
}



/**
 * @param {ClusterSecretValue} message
 * @param {Writer} writer
 */
export function writeClusterSecretValue(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.value && message.value.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.value);
    }
}


/**
 * @param {ClusterSecretValue} message
 * @returns {Uint8Array}
 */
export function encodeClusterSecretValue(message) {
    const writer = Writer.create();
    writeClusterSecretValue(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterSecretValue}
 */
function decodeClusterSecretValueMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, value: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.value = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterSecretValue}
 */
export function decodeClusterSecretValue(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterSecretValueMessage(reader);
}



/**
 * @param {ClusterSecretsResponse} message
 * @param {Writer} writer
 */
export function writeClusterSecretsResponse(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeClusterSecretValue(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ClusterSecretsResponse} message
 * @returns {Uint8Array}
 */
export function encodeClusterSecretsResponse(message) {
    const writer = Writer.create();
    writeClusterSecretsResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterSecretsResponse}
 */
function decodeClusterSecretsResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeClusterSecretValueMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterSecretsResponse}
 */
export function decodeClusterSecretsResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterSecretsResponseMessage(reader);
}



/**
 * @param {ClusterConfigsRequest} message
 * @param {Writer} writer
 */
export function writeClusterConfigsRequest(message, writer) {
    if (message.ids) {
        const packedWriter = Writer.create();
        for (const item of message.ids) {
            packedWriter.int32(item);
        }
        if (packedWriter.len > 0) {
            writer.uint32(tag(1, WIRE.LDELIM)).bytes(packedWriter.finish());
        }
    }
}


/**
 * @param {ClusterConfigsRequest} message
 * @returns {Uint8Array}
 */
export function encodeClusterConfigsRequest(message) {
    const writer = Writer.create();
    writeClusterConfigsRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterConfigsRequest}
 */
function decodeClusterConfigsRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {ids: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                const end2 = reader.uint32() + reader.pos;
                while (reader.pos < end2) {
                    message.ids.push(reader.int32());
                }
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterConfigsRequest}
 */
export function decodeClusterConfigsRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterConfigsRequestMessage(reader);
}



/**
 * @param {ClusterConfigValue} message
 * @param {Writer} writer
 */
export function writeClusterConfigValue(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.value !== undefined && message.value !== null && message.value !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.value);
    }
}


/**
 * @param {ClusterConfigValue} message
 * @returns {Uint8Array}
 */
export function encodeClusterConfigValue(message) {
    const writer = Writer.create();
    writeClusterConfigValue(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterConfigValue}
 */
function decodeClusterConfigValueMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, value: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.value = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterConfigValue}
 */
export function decodeClusterConfigValue(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterConfigValueMessage(reader);
}



/**
 * @param {ClusterConfigsResponse} message
 * @param {Writer} writer
 */
export function writeClusterConfigsResponse(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeClusterConfigValue(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ClusterConfigsResponse} message
 * @returns {Uint8Array}
 */
export function encodeClusterConfigsResponse(message) {
    const writer = Writer.create();
    writeClusterConfigsResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterConfigsResponse}
 */
function decodeClusterConfigsResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeClusterConfigValueMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterConfigsResponse}
 */
export function decodeClusterConfigsResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterConfigsResponseMessage(reader);
}



/**
 * @param {SecretValue} message
 * @param {Writer} writer
 */
export function writeSecretValue(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
}


/**
 * @param {SecretValue} message
 * @returns {Uint8Array}
 */
export function encodeSecretValue(message) {
    const writer = Writer.create();
    writeSecretValue(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretValue}
 */
function decodeSecretValueMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretValue}
 */
export function decodeSecretValue(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretValueMessage(reader);
}



/**
 * @param {DynamicConfiguration} message
 * @param {Writer} writer
 */
export function writeDynamicConfiguration(message, writer) {
    if (message.webListen !== undefined && message.webListen !== null && message.webListen !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.webListen);
    }
    if (message.webHttpOnly === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.webHttpOnly);
    }
    if (message.clusterListen !== undefined && message.clusterListen !== null && message.clusterListen !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.clusterListen);
    }
    if (message.enrollmentListen !== undefined && message.enrollmentListen !== null && message.enrollmentListen !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.enrollmentListen);
    }
    if (message.acmeHosts && message.acmeHosts.length > 0) {
        for (const item of message.acmeHosts) {
            writer.uint32(tag(5, WIRE.LDELIM)).string(item);
        }
    }
    if (message.acmeEmail !== undefined && message.acmeEmail !== null && message.acmeEmail !== "") {
        writer.uint32(tag(6, WIRE.LDELIM)).string(message.acmeEmail);
    }
    if (message.githubToken !== undefined && message.githubToken !== null) {
        writer.uint32(tag(7, WIRE.LDELIM)).fork();
        writeSecretValue(message.githubToken, writer);
        writer.ldelim();
    }
    if (message.backupS3AccessKeyId !== undefined && message.backupS3AccessKeyId !== null && message.backupS3AccessKeyId !== "") {
        writer.uint32(tag(8, WIRE.LDELIM)).string(message.backupS3AccessKeyId);
    }
    if (message.backupS3SecretAccessKey !== undefined && message.backupS3SecretAccessKey !== null) {
        writer.uint32(tag(9, WIRE.LDELIM)).fork();
        writeSecretValue(message.backupS3SecretAccessKey, writer);
        writer.ldelim();
    }
    if (message.backupS3Bucket !== undefined && message.backupS3Bucket !== null && message.backupS3Bucket !== "") {
        writer.uint32(tag(10, WIRE.LDELIM)).string(message.backupS3Bucket);
    }
    if (message.backupS3Path !== undefined && message.backupS3Path !== null && message.backupS3Path !== "") {
        writer.uint32(tag(11, WIRE.LDELIM)).string(message.backupS3Path);
    }
    if (message.backupS3Region !== undefined && message.backupS3Region !== null && message.backupS3Region !== "") {
        writer.uint32(tag(12, WIRE.LDELIM)).string(message.backupS3Region);
    }
    if (message.backupS3Endpoint !== undefined && message.backupS3Endpoint !== null && message.backupS3Endpoint !== "") {
        writer.uint32(tag(13, WIRE.LDELIM)).string(message.backupS3Endpoint);
    }
    if (message.backupEnabled === true) {
        writer.uint32(tag(14, WIRE.VARINT)).bool(message.backupEnabled);
    }
}


/**
 * @param {DynamicConfiguration} message
 * @returns {Uint8Array}
 */
export function encodeDynamicConfiguration(message) {
    const writer = Writer.create();
    writeDynamicConfiguration(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DynamicConfiguration}
 */
function decodeDynamicConfigurationMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {webListen: "", webHttpOnly: false, clusterListen: "", enrollmentListen: "", acmeHosts: [], acmeEmail: "", githubToken: undefined, backupS3AccessKeyId: "", backupS3SecretAccessKey: undefined, backupS3Bucket: "", backupS3Path: "", backupS3Region: "", backupS3Endpoint: "", backupEnabled: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.webListen = reader.string();
                break;
            }
            case 2: {
                message.webHttpOnly = reader.bool();
                break;
            }
            case 3: {
                message.clusterListen = reader.string();
                break;
            }
            case 4: {
                message.enrollmentListen = reader.string();
                break;
            }
            case 5: {
                message.acmeHosts.push(reader.string());
                break;
            }
            case 6: {
                message.acmeEmail = reader.string();
                break;
            }
            case 7: {
                message.githubToken = decodeSecretValueMessage(reader, reader.uint32());
                break;
            }
            case 8: {
                message.backupS3AccessKeyId = reader.string();
                break;
            }
            case 9: {
                message.backupS3SecretAccessKey = decodeSecretValueMessage(reader, reader.uint32());
                break;
            }
            case 10: {
                message.backupS3Bucket = reader.string();
                break;
            }
            case 11: {
                message.backupS3Path = reader.string();
                break;
            }
            case 12: {
                message.backupS3Region = reader.string();
                break;
            }
            case 13: {
                message.backupS3Endpoint = reader.string();
                break;
            }
            case 14: {
                message.backupEnabled = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DynamicConfiguration}
 */
export function decodeDynamicConfiguration(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDynamicConfigurationMessage(reader);
}



/**
 * @param {ConfigUpdateRequest} message
 * @param {Writer} writer
 */
export function writeConfigUpdateRequest(message, writer) {
    if (message.values && message.values.length > 0) {
        for (const item of message.values) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeConfigValueUpdate(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ConfigUpdateRequest} message
 * @returns {Uint8Array}
 */
export function encodeConfigUpdateRequest(message) {
    const writer = Writer.create();
    writeConfigUpdateRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ConfigUpdateRequest}
 */
function decodeConfigUpdateRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {values: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.values.push(decodeConfigValueUpdateMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ConfigUpdateRequest}
 */
export function decodeConfigUpdateRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeConfigUpdateRequestMessage(reader);
}



/**
 * @param {ConfigValueUpdate} message
 * @param {Writer} writer
 */
export function writeConfigValueUpdate(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
    if (message.value !== undefined && message.value !== null && message.value !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.value);
    }
}


/**
 * @param {ConfigValueUpdate} message
 * @returns {Uint8Array}
 */
export function encodeConfigValueUpdate(message) {
    const writer = Writer.create();
    writeConfigValueUpdate(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ConfigValueUpdate}
 */
function decodeConfigValueUpdateMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "", value: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            case 2: {
                message.value = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ConfigValueUpdate}
 */
export function decodeConfigValueUpdate(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeConfigValueUpdateMessage(reader);
}



/**
 * @param {EmptyRequest} message
 * @param {Writer} writer
 */
export function writeEmptyRequest(message, writer) {
}


/**
 * @param {EmptyRequest} message
 * @returns {Uint8Array}
 */
export function encodeEmptyRequest(message) {
    const writer = Writer.create();
    writeEmptyRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EmptyRequest}
 */
function decodeEmptyRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = { };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EmptyRequest}
 */
export function decodeEmptyRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEmptyRequestMessage(reader);
}



/**
 * @param {MasterPasswordRequest} message
 * @param {Writer} writer
 */
export function writeMasterPasswordRequest(message, writer) {
    if (message.password !== undefined && message.password !== null && message.password !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.password);
    }
    if (message.username !== undefined && message.username !== null && message.username !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.username);
    }
}


/**
 * @param {MasterPasswordRequest} message
 * @returns {Uint8Array}
 */
export function encodeMasterPasswordRequest(message) {
    const writer = Writer.create();
    writeMasterPasswordRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {MasterPasswordRequest}
 */
function decodeMasterPasswordRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {password: "", username: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.password = reader.string();
                break;
            }
            case 2: {
                message.username = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {MasterPasswordRequest}
 */
export function decodeMasterPasswordRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeMasterPasswordRequestMessage(reader);
}



/**
 * @param {MasterPasswordVerifyRequest} message
 * @param {Writer} writer
 */
export function writeMasterPasswordVerifyRequest(message, writer) {
    if (message.password !== undefined && message.password !== null && message.password !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.password);
    }
}


/**
 * @param {MasterPasswordVerifyRequest} message
 * @returns {Uint8Array}
 */
export function encodeMasterPasswordVerifyRequest(message) {
    const writer = Writer.create();
    writeMasterPasswordVerifyRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {MasterPasswordVerifyRequest}
 */
function decodeMasterPasswordVerifyRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {password: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.password = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {MasterPasswordVerifyRequest}
 */
export function decodeMasterPasswordVerifyRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeMasterPasswordVerifyRequestMessage(reader);
}



/**
 * @param {MasterPasswordSaveRequest} message
 * @param {Writer} writer
 */
export function writeMasterPasswordSaveRequest(message, writer) {
    if (message.password !== undefined && message.password !== null && message.password !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.password);
    }
}


/**
 * @param {MasterPasswordSaveRequest} message
 * @returns {Uint8Array}
 */
export function encodeMasterPasswordSaveRequest(message) {
    const writer = Writer.create();
    writeMasterPasswordSaveRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {MasterPasswordSaveRequest}
 */
function decodeMasterPasswordSaveRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {password: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.password = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {MasterPasswordSaveRequest}
 */
export function decodeMasterPasswordSaveRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeMasterPasswordSaveRequestMessage(reader);
}



/**
 * @param {LoginResponse} message
 * @param {Writer} writer
 */
export function writeLoginResponse(message, writer) {
    if (message.token !== undefined && message.token !== null && message.token !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.token);
    }
    if (message.userId !== undefined && message.userId !== null && message.userId !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.userId);
    }
    if (message.scopes && message.scopes.length > 0) {
        for (const item of message.scopes) {
            writer.uint32(tag(3, WIRE.LDELIM)).string(item);
        }
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.name);
    }
    if (message.expiry instanceof Date && message.expiry.getTime() !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int64(Math.trunc(message.expiry.getTime()));
    }
}


/**
 * @param {LoginResponse} message
 * @returns {Uint8Array}
 */
export function encodeLoginResponse(message) {
    const writer = Writer.create();
    writeLoginResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {LoginResponse}
 */
function decodeLoginResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {token: "", userId: 0, scopes: [], name: "", expiry: new Date(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.token = reader.string();
                break;
            }
            case 2: {
                message.userId = reader.int32();
                break;
            }
            case 3: {
                message.scopes.push(reader.string());
                break;
            }
            case 4: {
                message.name = reader.string();
                break;
            }
            case 5: {
                message.expiry = new Date(readInt64(reader, "int64"));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {LoginResponse}
 */
export function decodeLoginResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeLoginResponseMessage(reader);
}



/**
 * @param {WebAuthNOptionsResponse} message
 * @param {Writer} writer
 */
export function writeWebAuthNOptionsResponse(message, writer) {
    if (message.sessionId !== undefined && message.sessionId !== null && message.sessionId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.sessionId);
    }
    if (message.optionsJson && message.optionsJson.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.optionsJson);
    }
}


/**
 * @param {WebAuthNOptionsResponse} message
 * @returns {Uint8Array}
 */
export function encodeWebAuthNOptionsResponse(message) {
    const writer = Writer.create();
    writeWebAuthNOptionsResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {WebAuthNOptionsResponse}
 */
function decodeWebAuthNOptionsResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {sessionId: "", optionsJson: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.sessionId = reader.string();
                break;
            }
            case 2: {
                message.optionsJson = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {WebAuthNOptionsResponse}
 */
export function decodeWebAuthNOptionsResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeWebAuthNOptionsResponseMessage(reader);
}



/**
 * @param {WebAuthNFinishRequest} message
 * @param {Writer} writer
 */
export function writeWebAuthNFinishRequest(message, writer) {
    if (message.sessionId !== undefined && message.sessionId !== null && message.sessionId !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.sessionId);
    }
    if (message.credentialJson && message.credentialJson.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.credentialJson);
    }
}


/**
 * @param {WebAuthNFinishRequest} message
 * @returns {Uint8Array}
 */
export function encodeWebAuthNFinishRequest(message) {
    const writer = Writer.create();
    writeWebAuthNFinishRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {WebAuthNFinishRequest}
 */
function decodeWebAuthNFinishRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {sessionId: "", credentialJson: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.sessionId = reader.string();
                break;
            }
            case 2: {
                message.credentialJson = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {WebAuthNFinishRequest}
 */
export function decodeWebAuthNFinishRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeWebAuthNFinishRequestMessage(reader);
}



/**
 * @param {SecretMeta} message
 * @param {Writer} writer
 */
export function writeSecretMeta(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.group !== undefined && message.group !== null && message.group !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.group);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
    if (message.updatedAt instanceof Date && message.updatedAt.getTime() !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int64(Math.trunc(message.updatedAt.getTime()));
    }
    if (message.updatedBy !== undefined && message.updatedBy !== null && message.updatedBy !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int32(message.updatedBy);
    }
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(6, WIRE.VARINT)).int32(message.id);
    }
    if (message.deleted === true) {
        writer.uint32(tag(7, WIRE.VARINT)).bool(message.deleted);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(8, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {SecretMeta} message
 * @returns {Uint8Array}
 */
export function encodeSecretMeta(message) {
    const writer = Writer.create();
    writeSecretMeta(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretMeta}
 */
function decodeSecretMetaMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", group: "", createdAt: new Date(0), updatedAt: new Date(0), updatedBy: 0, id: 0, deleted: false, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.group = reader.string();
                break;
            }
            case 3: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 4: {
                message.updatedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 5: {
                message.updatedBy = reader.int32();
                break;
            }
            case 6: {
                message.id = reader.int32();
                break;
            }
            case 7: {
                message.deleted = reader.bool();
                break;
            }
            case 8: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretMeta}
 */
export function decodeSecretMeta(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretMetaMessage(reader);
}



/**
 * @param {SecretList} message
 * @param {Writer} writer
 */
export function writeSecretList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeSecretMeta(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {SecretList} message
 * @returns {Uint8Array}
 */
export function encodeSecretList(message) {
    const writer = Writer.create();
    writeSecretList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretList}
 */
function decodeSecretListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeSecretMetaMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretList}
 */
export function decodeSecretList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretListMessage(reader);
}



/**
 * @param {SecretSetRequest} message
 * @param {Writer} writer
 */
export function writeSecretSetRequest(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.group !== undefined && message.group !== null && message.group !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.group);
    }
    if (message.value && message.value.length > 0) {
        writer.uint32(tag(3, WIRE.LDELIM)).bytes(message.value);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {SecretSetRequest} message
 * @returns {Uint8Array}
 */
export function encodeSecretSetRequest(message) {
    const writer = Writer.create();
    writeSecretSetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretSetRequest}
 */
function decodeSecretSetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", group: "", value: new Uint8Array(0), spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.group = reader.string();
                break;
            }
            case 3: {
                message.value = reader.bytes();
                break;
            }
            case 4: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretSetRequest}
 */
export function decodeSecretSetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretSetRequestMessage(reader);
}



/**
 * @param {SecretDeleteRequest} message
 * @param {Writer} writer
 */
export function writeSecretDeleteRequest(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {SecretDeleteRequest} message
 * @returns {Uint8Array}
 */
export function encodeSecretDeleteRequest(message) {
    const writer = Writer.create();
    writeSecretDeleteRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretDeleteRequest}
 */
function decodeSecretDeleteRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretDeleteRequest}
 */
export function decodeSecretDeleteRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretDeleteRequestMessage(reader);
}



/**
 * @param {SecretRevealRequest} message
 * @param {Writer} writer
 */
export function writeSecretRevealRequest(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {SecretRevealRequest} message
 * @returns {Uint8Array}
 */
export function encodeSecretRevealRequest(message) {
    const writer = Writer.create();
    writeSecretRevealRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretRevealRequest}
 */
function decodeSecretRevealRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretRevealRequest}
 */
export function decodeSecretRevealRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretRevealRequestMessage(reader);
}



/**
 * @param {SecretRevealResponse} message
 * @param {Writer} writer
 */
export function writeSecretRevealResponse(message, writer) {
    if (message.value && message.value.length > 0) {
        writer.uint32(tag(1, WIRE.LDELIM)).bytes(message.value);
    }
}


/**
 * @param {SecretRevealResponse} message
 * @returns {Uint8Array}
 */
export function encodeSecretRevealResponse(message) {
    const writer = Writer.create();
    writeSecretRevealResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretRevealResponse}
 */
function decodeSecretRevealResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {value: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.value = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretRevealResponse}
 */
export function decodeSecretRevealResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretRevealResponseMessage(reader);
}



/**
 * @param {SecretsStatusResponse} message
 * @param {Writer} writer
 */
export function writeSecretsStatusResponse(message, writer) {
    if (message.unlocked === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.unlocked);
    }
    if (message.recoveryConfigured === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.recoveryConfigured);
    }
}


/**
 * @param {SecretsStatusResponse} message
 * @returns {Uint8Array}
 */
export function encodeSecretsStatusResponse(message) {
    const writer = Writer.create();
    writeSecretsStatusResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretsStatusResponse}
 */
function decodeSecretsStatusResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {unlocked: false, recoveryConfigured: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.unlocked = reader.bool();
                break;
            }
            case 2: {
                message.recoveryConfigured = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretsStatusResponse}
 */
export function decodeSecretsStatusResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretsStatusResponseMessage(reader);
}



/**
 * @param {SecretRecoveryCodeResponse} message
 * @param {Writer} writer
 */
export function writeSecretRecoveryCodeResponse(message, writer) {
    if (message.code !== undefined && message.code !== null && message.code !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.code);
    }
}


/**
 * @param {SecretRecoveryCodeResponse} message
 * @returns {Uint8Array}
 */
export function encodeSecretRecoveryCodeResponse(message) {
    const writer = Writer.create();
    writeSecretRecoveryCodeResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretRecoveryCodeResponse}
 */
function decodeSecretRecoveryCodeResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {code: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.code = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretRecoveryCodeResponse}
 */
export function decodeSecretRecoveryCodeResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretRecoveryCodeResponseMessage(reader);
}



/**
 * @param {SecretUnlockRequest} message
 * @param {Writer} writer
 */
export function writeSecretUnlockRequest(message, writer) {
    if (message.code !== undefined && message.code !== null && message.code !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.code);
    }
}


/**
 * @param {SecretUnlockRequest} message
 * @returns {Uint8Array}
 */
export function encodeSecretUnlockRequest(message) {
    const writer = Writer.create();
    writeSecretUnlockRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretUnlockRequest}
 */
function decodeSecretUnlockRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {code: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.code = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretUnlockRequest}
 */
export function decodeSecretUnlockRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretUnlockRequestMessage(reader);
}



/**
 * @param {UserConfig} message
 * @param {Writer} writer
 */
export function writeUserConfig(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.group !== undefined && message.group !== null && message.group !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.group);
    }
    if (message.value !== undefined && message.value !== null && message.value !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.value);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
    if (message.updatedAt instanceof Date && message.updatedAt.getTime() !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int64(Math.trunc(message.updatedAt.getTime()));
    }
    if (message.updatedBy !== undefined && message.updatedBy !== null && message.updatedBy !== 0) {
        writer.uint32(tag(6, WIRE.VARINT)).int32(message.updatedBy);
    }
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(7, WIRE.VARINT)).int32(message.id);
    }
    if (message.deleted === true) {
        writer.uint32(tag(8, WIRE.VARINT)).bool(message.deleted);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(9, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {UserConfig} message
 * @returns {Uint8Array}
 */
export function encodeUserConfig(message) {
    const writer = Writer.create();
    writeUserConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfig}
 */
function decodeUserConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", group: "", value: "", createdAt: new Date(0), updatedAt: new Date(0), updatedBy: 0, id: 0, deleted: false, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.group = reader.string();
                break;
            }
            case 3: {
                message.value = reader.string();
                break;
            }
            case 4: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 5: {
                message.updatedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 6: {
                message.updatedBy = reader.int32();
                break;
            }
            case 7: {
                message.id = reader.int32();
                break;
            }
            case 8: {
                message.deleted = reader.bool();
                break;
            }
            case 9: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfig}
 */
export function decodeUserConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigMessage(reader);
}



/**
 * @param {UserConfigList} message
 * @param {Writer} writer
 */
export function writeUserConfigList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeUserConfig(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {UserConfigList} message
 * @returns {Uint8Array}
 */
export function encodeUserConfigList(message) {
    const writer = Writer.create();
    writeUserConfigList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfigList}
 */
function decodeUserConfigListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeUserConfigMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfigList}
 */
export function decodeUserConfigList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigListMessage(reader);
}



/**
 * @param {UserConfigSetRequest} message
 * @param {Writer} writer
 */
export function writeUserConfigSetRequest(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.group !== undefined && message.group !== null && message.group !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.group);
    }
    if (message.value !== undefined && message.value !== null && message.value !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.value);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {UserConfigSetRequest} message
 * @returns {Uint8Array}
 */
export function encodeUserConfigSetRequest(message) {
    const writer = Writer.create();
    writeUserConfigSetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfigSetRequest}
 */
function decodeUserConfigSetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", group: "", value: "", spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.group = reader.string();
                break;
            }
            case 3: {
                message.value = reader.string();
                break;
            }
            case 4: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfigSetRequest}
 */
export function decodeUserConfigSetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigSetRequestMessage(reader);
}



/**
 * @param {UserConfigDeleteRequest} message
 * @param {Writer} writer
 */
export function writeUserConfigDeleteRequest(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {UserConfigDeleteRequest} message
 * @returns {Uint8Array}
 */
export function encodeUserConfigDeleteRequest(message) {
    const writer = Writer.create();
    writeUserConfigDeleteRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfigDeleteRequest}
 */
function decodeUserConfigDeleteRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfigDeleteRequest}
 */
export function decodeUserConfigDeleteRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigDeleteRequestMessage(reader);
}



/**
 * @param {AssetMeta} message
 * @param {Writer} writer
 */
export function writeAssetMeta(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.version);
    }
    if (message.format !== undefined && message.format !== null && message.format !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.format);
    }
    if (message.location !== undefined && message.location !== null && message.location !== "") {
        writer.uint32(tag(5, WIRE.LDELIM)).string(message.location);
    }
    if (message.sizeBytes !== undefined && message.sizeBytes !== null && message.sizeBytes !== 0) {
        writer.uint32(tag(6, WIRE.VARINT)).int32(message.sizeBytes);
    }
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(7, WIRE.VARINT)).int32(message.id);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(8, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {AssetMeta} message
 * @returns {Uint8Array}
 */
export function encodeAssetMeta(message) {
    const writer = Writer.create();
    writeAssetMeta(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AssetMeta}
 */
function decodeAssetMetaMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "", createdAt: new Date(0), version: 0, format: "", location: "", sizeBytes: 0, id: 0, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            case 2: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 3: {
                message.version = reader.int32();
                break;
            }
            case 4: {
                message.format = reader.string();
                break;
            }
            case 5: {
                message.location = reader.string();
                break;
            }
            case 6: {
                message.sizeBytes = reader.int32();
                break;
            }
            case 7: {
                message.id = reader.int32();
                break;
            }
            case 8: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AssetMeta}
 */
export function decodeAssetMeta(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetMetaMessage(reader);
}



/**
 * @param {Asset} message
 * @param {Writer} writer
 */
export function writeAsset(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.version);
    }
    if (message.format !== undefined && message.format !== null && message.format !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.format);
    }
    if (message.location !== undefined && message.location !== null && message.location !== "") {
        writer.uint32(tag(5, WIRE.LDELIM)).string(message.location);
    }
    if (message.blob && message.blob.length > 0) {
        writer.uint32(tag(6, WIRE.LDELIM)).bytes(message.blob);
    }
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(7, WIRE.VARINT)).int32(message.id);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(8, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {Asset} message
 * @returns {Uint8Array}
 */
export function encodeAsset(message) {
    const writer = Writer.create();
    writeAsset(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {Asset}
 */
function decodeAssetMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "", createdAt: new Date(0), version: 0, format: "", location: "", blob: new Uint8Array(0), id: 0, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            case 2: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 3: {
                message.version = reader.int32();
                break;
            }
            case 4: {
                message.format = reader.string();
                break;
            }
            case 5: {
                message.location = reader.string();
                break;
            }
            case 6: {
                message.blob = reader.bytes();
                break;
            }
            case 7: {
                message.id = reader.int32();
                break;
            }
            case 8: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {Asset}
 */
export function decodeAsset(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetMessage(reader);
}



/**
 * @param {AssetList} message
 * @param {Writer} writer
 */
export function writeAssetList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeAssetMeta(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {AssetList} message
 * @returns {Uint8Array}
 */
export function encodeAssetList(message) {
    const writer = Writer.create();
    writeAssetList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AssetList}
 */
function decodeAssetListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeAssetMetaMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AssetList}
 */
export function decodeAssetList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetListMessage(reader);
}



/**
 * @param {AssetGetRequest} message
 * @param {Writer} writer
 */
export function writeAssetGetRequest(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
}


/**
 * @param {AssetGetRequest} message
 * @returns {Uint8Array}
 */
export function encodeAssetGetRequest(message) {
    const writer = Writer.create();
    writeAssetGetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AssetGetRequest}
 */
function decodeAssetGetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "", version: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AssetGetRequest}
 */
export function decodeAssetGetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetGetRequestMessage(reader);
}



/**
 * @param {AssetSetRequest} message
 * @param {Writer} writer
 */
export function writeAssetSetRequest(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
    if (message.format !== undefined && message.format !== null && message.format !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.format);
    }
    if (message.blob && message.blob.length > 0) {
        writer.uint32(tag(3, WIRE.LDELIM)).bytes(message.blob);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {AssetSetRequest} message
 * @returns {Uint8Array}
 */
export function encodeAssetSetRequest(message) {
    const writer = Writer.create();
    writeAssetSetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AssetSetRequest}
 */
function decodeAssetSetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "", format: "", blob: new Uint8Array(0), spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            case 2: {
                message.format = reader.string();
                break;
            }
            case 3: {
                message.blob = reader.bytes();
                break;
            }
            case 4: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AssetSetRequest}
 */
export function decodeAssetSetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetSetRequestMessage(reader);
}



/**
 * @param {AssetDeleteRequest} message
 * @param {Writer} writer
 */
export function writeAssetDeleteRequest(message, writer) {
    if (message.key !== undefined && message.key !== null && message.key !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.key);
    }
}


/**
 * @param {AssetDeleteRequest} message
 * @returns {Uint8Array}
 */
export function encodeAssetDeleteRequest(message) {
    const writer = Writer.create();
    writeAssetDeleteRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AssetDeleteRequest}
 */
function decodeAssetDeleteRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {key: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.key = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AssetDeleteRequest}
 */
export function decodeAssetDeleteRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAssetDeleteRequestMessage(reader);
}



/**
 * @param {NixDockerBuildConfig} message
 * @param {Writer} writer
 */
export function writeNixDockerBuildConfig(message, writer) {
    if (message.repo !== undefined && message.repo !== null && message.repo !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.repo);
    }
    if (message.flake !== undefined && message.flake !== null && message.flake !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.flake);
    }
}


/**
 * @param {NixDockerBuildConfig} message
 * @returns {Uint8Array}
 */
export function encodeNixDockerBuildConfig(message) {
    const writer = Writer.create();
    writeNixDockerBuildConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {NixDockerBuildConfig}
 */
function decodeNixDockerBuildConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {repo: "", flake: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.repo = reader.string();
                break;
            }
            case 2: {
                message.flake = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {NixDockerBuildConfig}
 */
export function decodeNixDockerBuildConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeNixDockerBuildConfigMessage(reader);
}



/**
 * @param {GithubReleaseConfig} message
 * @param {Writer} writer
 */
export function writeGithubReleaseConfig(message, writer) {
    if (message.repo !== undefined && message.repo !== null && message.repo !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.repo);
    }
    if (message.asset !== undefined && message.asset !== null && message.asset !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.asset);
    }
    if (message.tag !== undefined && message.tag !== null && message.tag !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.tag);
    }
    if (message.downloadScript !== undefined && message.downloadScript !== null && message.downloadScript !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.downloadScript);
    }
}


/**
 * @param {GithubReleaseConfig} message
 * @returns {Uint8Array}
 */
export function encodeGithubReleaseConfig(message) {
    const writer = Writer.create();
    writeGithubReleaseConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {GithubReleaseConfig}
 */
function decodeGithubReleaseConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {repo: "", asset: "", tag: "", downloadScript: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.repo = reader.string();
                break;
            }
            case 2: {
                message.asset = reader.string();
                break;
            }
            case 3: {
                message.tag = reader.string();
                break;
            }
            case 4: {
                message.downloadScript = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {GithubReleaseConfig}
 */
export function decodeGithubReleaseConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeGithubReleaseConfigMessage(reader);
}



/**
 * @param {ContainerImageConfig} message
 * @param {Writer} writer
 */
export function writeContainerImageConfig(message, writer) {
    if (message.image !== undefined && message.image !== null && message.image !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.image);
    }
}


/**
 * @param {ContainerImageConfig} message
 * @returns {Uint8Array}
 */
export function encodeContainerImageConfig(message) {
    const writer = Writer.create();
    writeContainerImageConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ContainerImageConfig}
 */
function decodeContainerImageConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {image: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.image = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ContainerImageConfig}
 */
export function decodeContainerImageConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeContainerImageConfigMessage(reader);
}



/**
 * @param {PrepareConfig} message
 * @param {Writer} writer
 */
export function writePrepareConfig(message, writer) {
    if (message.githubRelease !== undefined && message.githubRelease !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeGithubReleaseConfig(message.githubRelease, writer);
        writer.ldelim();
    }
    if (message.containerImage !== undefined && message.containerImage !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writeContainerImageConfig(message.containerImage, writer);
        writer.ldelim();
    }
    if (message.nixDockerBuild !== undefined && message.nixDockerBuild !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeNixDockerBuildConfig(message.nixDockerBuild, writer);
        writer.ldelim();
    }
}


/**
 * @param {PrepareConfig} message
 * @returns {Uint8Array}
 */
export function encodePrepareConfig(message) {
    const writer = Writer.create();
    writePrepareConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {PrepareConfig}
 */
function decodePrepareConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {githubRelease: undefined, containerImage: undefined, nixDockerBuild: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 2: {
                message.githubRelease = decodeGithubReleaseConfigMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.containerImage = decodeContainerImageConfigMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.nixDockerBuild = decodeNixDockerBuildConfigMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {PrepareConfig}
 */
export function decodePrepareConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodePrepareConfigMessage(reader);
}



/**
 * @param {EnvVarValue} message
 * @param {Writer} writer
 */
export function writeEnvVarValue(message, writer) {
    if (message.secretId !== undefined && message.secretId !== null) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.secretId);
    }
    if (message.configId !== undefined && message.configId !== null) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.configId);
    }
    if (message.value !== undefined && message.value !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.value);
    }
}


/**
 * @param {EnvVarValue} message
 * @returns {Uint8Array}
 */
export function encodeEnvVarValue(message) {
    const writer = Writer.create();
    writeEnvVarValue(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {EnvVarValue}
 */
function decodeEnvVarValueMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {secretId: undefined, configId: undefined, value: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.secretId = reader.int32();
                break;
            }
            case 2: {
                message.configId = reader.int32();
                break;
            }
            case 3: {
                message.value = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {EnvVarValue}
 */
export function decodeEnvVarValue(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeEnvVarValueMessage(reader);
}



/**
 * @param {SystemdRunnerConfig} message
 * @param {Writer} writer
 */
export function writeSystemdRunnerConfig(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.binPath !== undefined && message.binPath !== null && message.binPath !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.binPath);
    }
}


/**
 * @param {SystemdRunnerConfig} message
 * @returns {Uint8Array}
 */
export function encodeSystemdRunnerConfig(message) {
    const writer = Writer.create();
    writeSystemdRunnerConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SystemdRunnerConfig}
 */
function decodeSystemdRunnerConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", binPath: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.binPath = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SystemdRunnerConfig}
 */
export function decodeSystemdRunnerConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSystemdRunnerConfigMessage(reader);
}



/**
 * @param {ContainerMount} message
 * @param {Writer} writer
 */
export function writeContainerMount(message, writer) {
    if (message.host !== undefined && message.host !== null && message.host !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.host);
    }
    if (message.container !== undefined && message.container !== null && message.container !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.container);
    }
    if (message.readonly === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.readonly);
    }
}


/**
 * @param {ContainerMount} message
 * @returns {Uint8Array}
 */
export function encodeContainerMount(message) {
    const writer = Writer.create();
    writeContainerMount(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ContainerMount}
 */
function decodeContainerMountMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {host: "", container: "", readonly: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.host = reader.string();
                break;
            }
            case 2: {
                message.container = reader.string();
                break;
            }
            case 3: {
                message.readonly = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ContainerMount}
 */
export function decodeContainerMount(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeContainerMountMessage(reader);
}



/**
 * @param {ContainerAssetMount} message
 * @param {Writer} writer
 */
export function writeContainerAssetMount(message, writer) {
    if (message.asset !== undefined && message.asset !== null && message.asset !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.asset);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
    if (message.path !== undefined && message.path !== null && message.path !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.path);
    }
    if (message.format !== undefined && message.format !== null && message.format !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.format);
    }
    if (message.assetId !== undefined && message.assetId !== null && message.assetId !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int32(message.assetId);
    }
}


/**
 * @param {ContainerAssetMount} message
 * @returns {Uint8Array}
 */
export function encodeContainerAssetMount(message) {
    const writer = Writer.create();
    writeContainerAssetMount(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ContainerAssetMount}
 */
function decodeContainerAssetMountMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {asset: "", version: 0, path: "", format: "", assetId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.asset = reader.string();
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            case 3: {
                message.path = reader.string();
                break;
            }
            case 4: {
                message.format = reader.string();
                break;
            }
            case 5: {
                message.assetId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ContainerAssetMount}
 */
export function decodeContainerAssetMount(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeContainerAssetMountMessage(reader);
}



/**
 * @param {ContainerRunnerConfig} message
 * @param {Writer} writer
 */
export function writeContainerRunnerConfig(message, writer) {
    if (message.user !== undefined && message.user !== null && message.user !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.user);
    }
    if (message.envVars && Object.keys(message.envVars).length > 0) {
        for (const [rawKey, value] of Object.entries(message.envVars)) {
            const key = rawKey;
            writer.uint32(tag(2, WIRE.LDELIM)).fork();
            writer.uint32(tag(1, WIRE.LDELIM)).string(key);
            if (value) {
                writer.uint32(tag(2, WIRE.LDELIM)).fork();
                writeEnvVarValue(value, writer);
                writer.ldelim();
            }
            writer.ldelim();
        }
    }
    if (message.command && message.command.length > 0) {
        for (const item of message.command) {
            writer.uint32(tag(3, WIRE.LDELIM)).string(item);
        }
    }
    if (message.workingDir !== undefined && message.workingDir !== null && message.workingDir !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.workingDir);
    }
    if (message.dataMountPath !== undefined && message.dataMountPath !== null && message.dataMountPath !== "") {
        writer.uint32(tag(5, WIRE.LDELIM)).string(message.dataMountPath);
    }
    if (message.disableDataVolume === true) {
        writer.uint32(tag(6, WIRE.VARINT)).bool(message.disableDataVolume);
    }
    if (message.mounts && message.mounts.length > 0) {
        for (const item of message.mounts) {
            writer.uint32(tag(7, WIRE.LDELIM)).fork();
            writeContainerMount(item, writer);
            writer.ldelim();
        }
    }
    if (message.assetMounts && message.assetMounts.length > 0) {
        for (const item of message.assetMounts) {
            writer.uint32(tag(8, WIRE.LDELIM)).fork();
            writeContainerAssetMount(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ContainerRunnerConfig} message
 * @returns {Uint8Array}
 */
export function encodeContainerRunnerConfig(message) {
    const writer = Writer.create();
    writeContainerRunnerConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ContainerRunnerConfig}
 */
function decodeContainerRunnerConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {user: "", envVars: {}, command: [], workingDir: "", dataMountPath: "", disableDataVolume: false, mounts: [], assetMounts: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.user = reader.string();
                break;
            }
            case 2: {
                const end2 = reader.uint32() + reader.pos;
                let key = "";
                let value = undefined;
                while (reader.pos < end2) {
                    const tag2 = reader.uint32();
                    switch (tag2 >>> 3) {
                        case 1:
                            key = reader.string();
                            break;
                        case 2:
                            value = decodeEnvVarValueMessage(reader, reader.uint32());
                            break;
                        default:
                            reader.skipType(tag2 & 7);
                    }
                }
                if (!message.envVars) { message.envVars = {}; }
                message.envVars[String(key)] = value;
                break;
            }
            case 3: {
                message.command.push(reader.string());
                break;
            }
            case 4: {
                message.workingDir = reader.string();
                break;
            }
            case 5: {
                message.dataMountPath = reader.string();
                break;
            }
            case 6: {
                message.disableDataVolume = reader.bool();
                break;
            }
            case 7: {
                message.mounts.push(decodeContainerMountMessage(reader, reader.uint32()));
                break;
            }
            case 8: {
                message.assetMounts.push(decodeContainerAssetMountMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ContainerRunnerConfig}
 */
export function decodeContainerRunnerConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeContainerRunnerConfigMessage(reader);
}



/**
 * @param {RunnerConfig} message
 * @param {Writer} writer
 */
export function writeRunnerConfig(message, writer) {
    if (message.systemd !== undefined && message.systemd !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeSystemdRunnerConfig(message.systemd, writer);
        writer.ldelim();
    }
    if (message.container !== undefined && message.container !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writeContainerRunnerConfig(message.container, writer);
        writer.ldelim();
    }
}


/**
 * @param {RunnerConfig} message
 * @returns {Uint8Array}
 */
export function encodeRunnerConfig(message) {
    const writer = Writer.create();
    writeRunnerConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {RunnerConfig}
 */
function decodeRunnerConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {systemd: undefined, container: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 2: {
                message.systemd = decodeSystemdRunnerConfigMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.container = decodeContainerRunnerConfigMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {RunnerConfig}
 */
export function decodeRunnerConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeRunnerConfigMessage(reader);
}



/**
 * @param {State} message
 * @param {Writer} writer
 */
export function writeState(message, writer) {
    if (message.heartbeat === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.heartbeat);
    }
    if (message.deploymentsSnapshot !== undefined && message.deploymentsSnapshot !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentWithStatusSnapshot(message.deploymentsSnapshot, writer);
        writer.ldelim();
    }
    if (message.deploymentUpdate !== undefined && message.deploymentUpdate !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writeDeploymentWithStatus(message.deploymentUpdate, writer);
        writer.ldelim();
    }
    if (message.usersSnapshot && message.usersSnapshot.length > 0) {
        for (const item of message.usersSnapshot) {
            writer.uint32(tag(6, WIRE.LDELIM)).fork();
            writeUser(item, writer);
            writer.ldelim();
        }
    }
    if (message.userUpdate !== undefined && message.userUpdate !== null) {
        writer.uint32(tag(7, WIRE.LDELIM)).fork();
        writeUser(message.userUpdate, writer);
        writer.ldelim();
    }
    if (message.machinesSnapshot !== undefined && message.machinesSnapshot !== null) {
        writer.uint32(tag(8, WIRE.LDELIM)).fork();
        writeClusterMachineList(message.machinesSnapshot, writer);
        writer.ldelim();
    }
    if (message.machineUpdate !== undefined && message.machineUpdate !== null) {
        writer.uint32(tag(9, WIRE.LDELIM)).fork();
        writeClusterMachine(message.machineUpdate, writer);
        writer.ldelim();
    }
    if (message.enrollmentsSnapshot !== undefined && message.enrollmentsSnapshot !== null) {
        writer.uint32(tag(10, WIRE.LDELIM)).fork();
        writeEnrollmentRequestList(message.enrollmentsSnapshot, writer);
        writer.ldelim();
    }
    if (message.enrollmentUpdate !== undefined && message.enrollmentUpdate !== null) {
        writer.uint32(tag(11, WIRE.LDELIM)).fork();
        writeEnrollmentRequestStatus(message.enrollmentUpdate, writer);
        writer.ldelim();
    }
    if (message.secretsSnapshot !== undefined && message.secretsSnapshot !== null) {
        writer.uint32(tag(12, WIRE.LDELIM)).fork();
        writeSecretReferenceList(message.secretsSnapshot, writer);
        writer.ldelim();
    }
    if (message.secretUpdate !== undefined && message.secretUpdate !== null) {
        writer.uint32(tag(13, WIRE.LDELIM)).fork();
        writeSecretReference(message.secretUpdate, writer);
        writer.ldelim();
    }
    if (message.userConfigsSnapshot !== undefined && message.userConfigsSnapshot !== null) {
        writer.uint32(tag(14, WIRE.LDELIM)).fork();
        writeUserConfigReferenceList(message.userConfigsSnapshot, writer);
        writer.ldelim();
    }
    if (message.userConfigUpdate !== undefined && message.userConfigUpdate !== null) {
        writer.uint32(tag(15, WIRE.LDELIM)).fork();
        writeUserConfigReference(message.userConfigUpdate, writer);
        writer.ldelim();
    }
    if (message.secretsStatusSnapshot !== undefined && message.secretsStatusSnapshot !== null) {
        writer.uint32(tag(16, WIRE.LDELIM)).fork();
        writeSecretsStatusResponse(message.secretsStatusSnapshot, writer);
        writer.ldelim();
    }
    if (message.secretMetasSnapshot !== undefined && message.secretMetasSnapshot !== null) {
        writer.uint32(tag(17, WIRE.LDELIM)).fork();
        writeSecretList(message.secretMetasSnapshot, writer);
        writer.ldelim();
    }
    if (message.secretMetaUpdate !== undefined && message.secretMetaUpdate !== null) {
        writer.uint32(tag(18, WIRE.LDELIM)).fork();
        writeSecretMeta(message.secretMetaUpdate, writer);
        writer.ldelim();
    }
    if (message.userConfigValuesSnapshot !== undefined && message.userConfigValuesSnapshot !== null) {
        writer.uint32(tag(19, WIRE.LDELIM)).fork();
        writeUserConfigList(message.userConfigValuesSnapshot, writer);
        writer.ldelim();
    }
    if (message.userConfigValueUpdate !== undefined && message.userConfigValueUpdate !== null) {
        writer.uint32(tag(20, WIRE.LDELIM)).fork();
        writeUserConfig(message.userConfigValueUpdate, writer);
        writer.ldelim();
    }
    if (message.spacesSnapshot !== undefined && message.spacesSnapshot !== null) {
        writer.uint32(tag(21, WIRE.LDELIM)).fork();
        writeSpaceList(message.spacesSnapshot, writer);
        writer.ldelim();
    }
    if (message.spaceUpdate !== undefined && message.spaceUpdate !== null) {
        writer.uint32(tag(22, WIRE.LDELIM)).fork();
        writeSpace(message.spaceUpdate, writer);
        writer.ldelim();
    }
}


/**
 * @param {State} message
 * @returns {Uint8Array}
 */
export function encodeState(message) {
    const writer = Writer.create();
    writeState(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {State}
 */
function decodeStateMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {heartbeat: false, deploymentsSnapshot: undefined, deploymentUpdate: undefined, usersSnapshot: [], userUpdate: undefined, machinesSnapshot: undefined, machineUpdate: undefined, enrollmentsSnapshot: undefined, enrollmentUpdate: undefined, secretsSnapshot: undefined, secretUpdate: undefined, userConfigsSnapshot: undefined, userConfigUpdate: undefined, secretsStatusSnapshot: undefined, secretMetasSnapshot: undefined, secretMetaUpdate: undefined, userConfigValuesSnapshot: undefined, userConfigValueUpdate: undefined, spacesSnapshot: undefined, spaceUpdate: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.heartbeat = reader.bool();
                break;
            }
            case 2: {
                message.deploymentsSnapshot = decodeDeploymentWithStatusSnapshotMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.deploymentUpdate = decodeDeploymentWithStatusMessage(reader, reader.uint32());
                break;
            }
            case 6: {
                message.usersSnapshot.push(decodeUserMessage(reader, reader.uint32()));
                break;
            }
            case 7: {
                message.userUpdate = decodeUserMessage(reader, reader.uint32());
                break;
            }
            case 8: {
                message.machinesSnapshot = decodeClusterMachineListMessage(reader, reader.uint32());
                break;
            }
            case 9: {
                message.machineUpdate = decodeClusterMachineMessage(reader, reader.uint32());
                break;
            }
            case 10: {
                message.enrollmentsSnapshot = decodeEnrollmentRequestListMessage(reader, reader.uint32());
                break;
            }
            case 11: {
                message.enrollmentUpdate = decodeEnrollmentRequestStatusMessage(reader, reader.uint32());
                break;
            }
            case 12: {
                message.secretsSnapshot = decodeSecretReferenceListMessage(reader, reader.uint32());
                break;
            }
            case 13: {
                message.secretUpdate = decodeSecretReferenceMessage(reader, reader.uint32());
                break;
            }
            case 14: {
                message.userConfigsSnapshot = decodeUserConfigReferenceListMessage(reader, reader.uint32());
                break;
            }
            case 15: {
                message.userConfigUpdate = decodeUserConfigReferenceMessage(reader, reader.uint32());
                break;
            }
            case 16: {
                message.secretsStatusSnapshot = decodeSecretsStatusResponseMessage(reader, reader.uint32());
                break;
            }
            case 17: {
                message.secretMetasSnapshot = decodeSecretListMessage(reader, reader.uint32());
                break;
            }
            case 18: {
                message.secretMetaUpdate = decodeSecretMetaMessage(reader, reader.uint32());
                break;
            }
            case 19: {
                message.userConfigValuesSnapshot = decodeUserConfigListMessage(reader, reader.uint32());
                break;
            }
            case 20: {
                message.userConfigValueUpdate = decodeUserConfigMessage(reader, reader.uint32());
                break;
            }
            case 21: {
                message.spacesSnapshot = decodeSpaceListMessage(reader, reader.uint32());
                break;
            }
            case 22: {
                message.spaceUpdate = decodeSpaceMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {State}
 */
export function decodeState(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeStateMessage(reader);
}



/**
 * @param {Space} message
 * @param {Writer} writer
 */
export function writeSpace(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.name);
    }
    if (message.deleted === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.deleted);
    }
}


/**
 * @param {Space} message
 * @returns {Uint8Array}
 */
export function encodeSpace(message) {
    const writer = Writer.create();
    writeSpace(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {Space}
 */
function decodeSpaceMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, name: "", deleted: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.name = reader.string();
                break;
            }
            case 3: {
                message.deleted = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {Space}
 */
export function decodeSpace(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSpaceMessage(reader);
}



/**
 * @param {SpaceList} message
 * @param {Writer} writer
 */
export function writeSpaceList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeSpace(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {SpaceList} message
 * @returns {Uint8Array}
 */
export function encodeSpaceList(message) {
    const writer = Writer.create();
    writeSpaceList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SpaceList}
 */
function decodeSpaceListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeSpaceMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SpaceList}
 */
export function decodeSpaceList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSpaceListMessage(reader);
}



/**
 * @param {SpaceSetRequest} message
 * @param {Writer} writer
 */
export function writeSpaceSetRequest(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {SpaceSetRequest} message
 * @returns {Uint8Array}
 */
export function encodeSpaceSetRequest(message) {
    const writer = Writer.create();
    writeSpaceSetRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SpaceSetRequest}
 */
function decodeSpaceSetRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SpaceSetRequest}
 */
export function decodeSpaceSetRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSpaceSetRequestMessage(reader);
}



/**
 * @param {SpaceDeleteRequest} message
 * @param {Writer} writer
 */
export function writeSpaceDeleteRequest(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
}


/**
 * @param {SpaceDeleteRequest} message
 * @returns {Uint8Array}
 */
export function encodeSpaceDeleteRequest(message) {
    const writer = Writer.create();
    writeSpaceDeleteRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SpaceDeleteRequest}
 */
function decodeSpaceDeleteRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SpaceDeleteRequest}
 */
export function decodeSpaceDeleteRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSpaceDeleteRequestMessage(reader);
}



/**
 * @param {SecretReference} message
 * @param {Writer} writer
 */
export function writeSecretReference(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.name);
    }
    if (message.deleted === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.deleted);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {SecretReference} message
 * @returns {Uint8Array}
 */
export function encodeSecretReference(message) {
    const writer = Writer.create();
    writeSecretReference(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretReference}
 */
function decodeSecretReferenceMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, name: "", deleted: false, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.name = reader.string();
                break;
            }
            case 3: {
                message.deleted = reader.bool();
                break;
            }
            case 4: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretReference}
 */
export function decodeSecretReference(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretReferenceMessage(reader);
}



/**
 * @param {SecretReferenceList} message
 * @param {Writer} writer
 */
export function writeSecretReferenceList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeSecretReference(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {SecretReferenceList} message
 * @returns {Uint8Array}
 */
export function encodeSecretReferenceList(message) {
    const writer = Writer.create();
    writeSecretReferenceList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {SecretReferenceList}
 */
function decodeSecretReferenceListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeSecretReferenceMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {SecretReferenceList}
 */
export function decodeSecretReferenceList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeSecretReferenceListMessage(reader);
}



/**
 * @param {UserConfigReference} message
 * @param {Writer} writer
 */
export function writeUserConfigReference(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.name);
    }
    if (message.deleted === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.deleted);
    }
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {UserConfigReference} message
 * @returns {Uint8Array}
 */
export function encodeUserConfigReference(message) {
    const writer = Writer.create();
    writeUserConfigReference(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfigReference}
 */
function decodeUserConfigReferenceMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, name: "", deleted: false, spaceId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.name = reader.string();
                break;
            }
            case 3: {
                message.deleted = reader.bool();
                break;
            }
            case 4: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfigReference}
 */
export function decodeUserConfigReference(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigReferenceMessage(reader);
}



/**
 * @param {UserConfigReferenceList} message
 * @param {Writer} writer
 */
export function writeUserConfigReferenceList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeUserConfigReference(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {UserConfigReferenceList} message
 * @returns {Uint8Array}
 */
export function encodeUserConfigReferenceList(message) {
    const writer = Writer.create();
    writeUserConfigReferenceList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {UserConfigReferenceList}
 */
function decodeUserConfigReferenceListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeUserConfigReferenceMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {UserConfigReferenceList}
 */
export function decodeUserConfigReferenceList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserConfigReferenceListMessage(reader);
}



/**
 * @param {DeploymentWithStatusSnapshot} message
 * @param {Writer} writer
 */
export function writeDeploymentWithStatusSnapshot(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeDeploymentWithStatus(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {DeploymentWithStatusSnapshot} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentWithStatusSnapshot(message) {
    const writer = Writer.create();
    writeDeploymentWithStatusSnapshot(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentWithStatusSnapshot}
 */
function decodeDeploymentWithStatusSnapshotMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeDeploymentWithStatusMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentWithStatusSnapshot}
 */
export function decodeDeploymentWithStatusSnapshot(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentWithStatusSnapshotMessage(reader);
}



/**
 * @param {ClusterMachineList} message
 * @param {Writer} writer
 */
export function writeClusterMachineList(message, writer) {
    if (message.items && message.items.length > 0) {
        for (const item of message.items) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeClusterMachine(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ClusterMachineList} message
 * @returns {Uint8Array}
 */
export function encodeClusterMachineList(message) {
    const writer = Writer.create();
    writeClusterMachineList(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterMachineList}
 */
function decodeClusterMachineListMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {items: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.items.push(decodeClusterMachineMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterMachineList}
 */
export function decodeClusterMachineList(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterMachineListMessage(reader);
}



/**
 * @param {DeploymentHistoryEntry} message
 * @param {Writer} writer
 */
export function writeDeploymentHistoryEntry(message, writer) {
    if (message.config !== undefined && message.config !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentConfig(message.config, writer);
        writer.ldelim();
    }
    if (message.status !== undefined && message.status !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentStatus(message.status, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentHistoryEntry} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentHistoryEntry(message) {
    const writer = Writer.create();
    writeDeploymentHistoryEntry(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentHistoryEntry}
 */
function decodeDeploymentHistoryEntryMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {config: undefined, status: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.config = decodeDeploymentConfigMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.status = decodeDeploymentStatusMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentHistoryEntry}
 */
export function decodeDeploymentHistoryEntry(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentHistoryEntryMessage(reader);
}



/**
 * @param {DeploymentHistory} message
 * @param {Writer} writer
 */
export function writeDeploymentHistory(message, writer) {
    if (message.entries && message.entries.length > 0) {
        for (const item of message.entries) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeDeploymentHistoryEntry(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {DeploymentHistory} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentHistory(message) {
    const writer = Writer.create();
    writeDeploymentHistory(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentHistory}
 */
function decodeDeploymentHistoryMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {entries: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.entries.push(decodeDeploymentHistoryEntryMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentHistory}
 */
export function decodeDeploymentHistory(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentHistoryMessage(reader);
}



/**
 * @param {DeploymentUpdateRequest} message
 * @param {Writer} writer
 */
export function writeDeploymentUpdateRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(5, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.targetVersion !== undefined && message.targetVersion !== null && message.targetVersion !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.targetVersion);
    }
    if (message.stop === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.stop);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.version);
    }
    if (message.spec !== undefined && message.spec !== null) {
        writer.uint32(tag(6, WIRE.LDELIM)).fork();
        writeDeploymentSpec(message.spec, writer);
        writer.ldelim();
    }
    if (message.spaceId !== undefined && message.spaceId !== null) {
        writer.uint32(tag(7, WIRE.VARINT)).int32(message.spaceId);
    }
}


/**
 * @param {DeploymentUpdateRequest} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentUpdateRequest(message) {
    const writer = Writer.create();
    writeDeploymentUpdateRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentUpdateRequest}
 */
function decodeDeploymentUpdateRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, targetVersion: "", stop: false, version: 0, spec: undefined, spaceId: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 5: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.targetVersion = reader.string();
                break;
            }
            case 3: {
                message.stop = reader.bool();
                break;
            }
            case 4: {
                message.version = reader.int32();
                break;
            }
            case 6: {
                message.spec = decodeDeploymentSpecMessage(reader, reader.uint32());
                break;
            }
            case 7: {
                message.spaceId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentUpdateRequest}
 */
export function decodeDeploymentUpdateRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentUpdateRequestMessage(reader);
}



/**
 * @param {DeploymentCreateRequest} message
 * @param {Writer} writer
 */
export function writeDeploymentCreateRequest(message, writer) {
    if (message.configId !== undefined && message.configId !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentIdentifier(message.configId, writer);
        writer.ldelim();
    }
    if (message.spec !== undefined && message.spec !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentSpec(message.spec, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentCreateRequest} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentCreateRequest(message) {
    const writer = Writer.create();
    writeDeploymentCreateRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentCreateRequest}
 */
function decodeDeploymentCreateRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {configId: undefined, spec: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.configId = decodeDeploymentIdentifierMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.spec = decodeDeploymentSpecMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentCreateRequest}
 */
export function decodeDeploymentCreateRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentCreateRequestMessage(reader);
}



/**
 * @param {DeploymentHistoryRequest} message
 * @param {Writer} writer
 */
export function writeDeploymentHistoryRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentId);
    }
}


/**
 * @param {DeploymentHistoryRequest} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentHistoryRequest(message) {
    const writer = Writer.create();
    writeDeploymentHistoryRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentHistoryRequest}
 */
function decodeDeploymentHistoryRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentId = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentHistoryRequest}
 */
export function decodeDeploymentHistoryRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentHistoryRequestMessage(reader);
}



/**
 * @param {PrepareOutputRequest} message
 * @param {Writer} writer
 */
export function writePrepareOutputRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
}


/**
 * @param {PrepareOutputRequest} message
 * @returns {Uint8Array}
 */
export function encodePrepareOutputRequest(message) {
    const writer = Writer.create();
    writePrepareOutputRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {PrepareOutputRequest}
 */
function decodePrepareOutputRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, version: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 3: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {PrepareOutputRequest}
 */
export function decodePrepareOutputRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodePrepareOutputRequestMessage(reader);
}



/**
 * @param {PrepareOutputChunk} message
 * @param {Writer} writer
 */
export function writePrepareOutputChunk(message, writer) {
    if (message.data && message.data.length > 0) {
        writer.uint32(tag(1, WIRE.LDELIM)).bytes(message.data);
    }
}


/**
 * @param {PrepareOutputChunk} message
 * @returns {Uint8Array}
 */
export function encodePrepareOutputChunk(message) {
    const writer = Writer.create();
    writePrepareOutputChunk(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {PrepareOutputChunk}
 */
function decodePrepareOutputChunkMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {data: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.data = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {PrepareOutputChunk}
 */
export function decodePrepareOutputChunk(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodePrepareOutputChunkMessage(reader);
}



/**
 * @param {RunOutputRequest} message
 * @param {Writer} writer
 */
export function writeRunOutputRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
}


/**
 * @param {RunOutputRequest} message
 * @returns {Uint8Array}
 */
export function encodeRunOutputRequest(message) {
    const writer = Writer.create();
    writeRunOutputRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {RunOutputRequest}
 */
function decodeRunOutputRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, version: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 3: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {RunOutputRequest}
 */
export function decodeRunOutputRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeRunOutputRequestMessage(reader);
}



/**
 * @param {DeploymentLogRequest} message
 * @param {Writer} writer
 */
export function writeDeploymentLogRequest(message, writer) {
    if (message.runnerOutput !== undefined && message.runnerOutput !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeRunOutputRequest(message.runnerOutput, writer);
        writer.ldelim();
    }
    if (message.preparerOutput !== undefined && message.preparerOutput !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writePrepareOutputRequest(message.preparerOutput, writer);
        writer.ldelim();
    }
    if (message.requestId !== undefined && message.requestId !== null && message.requestId !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.requestId);
    }
}


/**
 * @param {DeploymentLogRequest} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentLogRequest(message) {
    const writer = Writer.create();
    writeDeploymentLogRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentLogRequest}
 */
function decodeDeploymentLogRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {runnerOutput: undefined, preparerOutput: undefined, requestId: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.runnerOutput = decodeRunOutputRequestMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.preparerOutput = decodePrepareOutputRequestMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.requestId = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentLogRequest}
 */
export function decodeDeploymentLogRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentLogRequestMessage(reader);
}



/**
 * @param {LogSearchRequest} message
 * @param {Writer} writer
 */
export function writeLogSearchRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.timeStart instanceof Date && message.timeStart.getTime() !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int64(Math.trunc(message.timeStart.getTime()));
    }
    if (message.timeEnd instanceof Date && message.timeEnd.getTime() !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int64(Math.trunc(message.timeEnd.getTime()));
    }
    if (message.levelMin !== undefined && message.levelMin !== null && message.levelMin !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.levelMin);
    }
    if (message.searchKeys && Object.keys(message.searchKeys).length > 0) {
        for (const [rawKey, value] of Object.entries(message.searchKeys)) {
            const key = rawKey;
            writer.uint32(tag(5, WIRE.LDELIM)).fork();
            writer.uint32(tag(1, WIRE.LDELIM)).string(key);
            if (value !== undefined && value !== null && value !== "") {
                writer.uint32(tag(2, WIRE.LDELIM)).string(value);
            }
            writer.ldelim();
        }
    }
    if (message.requestId !== undefined && message.requestId !== null && message.requestId !== "") {
        writer.uint32(tag(6, WIRE.LDELIM)).string(message.requestId);
    }
    if (message.logLineLimit !== undefined && message.logLineLimit !== null && message.logLineLimit !== 0) {
        writer.uint32(tag(7, WIRE.VARINT)).int32(message.logLineLimit);
    }
}


/**
 * @param {LogSearchRequest} message
 * @returns {Uint8Array}
 */
export function encodeLogSearchRequest(message) {
    const writer = Writer.create();
    writeLogSearchRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {LogSearchRequest}
 */
function decodeLogSearchRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, timeStart: new Date(0), timeEnd: new Date(0), levelMin: "", searchKeys: {}, requestId: "", logLineLimit: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.timeStart = new Date(readInt64(reader, "int64"));
                break;
            }
            case 3: {
                message.timeEnd = new Date(readInt64(reader, "int64"));
                break;
            }
            case 4: {
                message.levelMin = reader.string();
                break;
            }
            case 5: {
                const end2 = reader.uint32() + reader.pos;
                let key = "";
                let value = "";
                while (reader.pos < end2) {
                    const tag2 = reader.uint32();
                    switch (tag2 >>> 3) {
                        case 1:
                            key = reader.string();
                            break;
                        case 2:
                            value = reader.string();
                            break;
                        default:
                            reader.skipType(tag2 & 7);
                    }
                }
                if (!message.searchKeys) { message.searchKeys = {}; }
                message.searchKeys[String(key)] = value;
                break;
            }
            case 6: {
                message.requestId = reader.string();
                break;
            }
            case 7: {
                message.logLineLimit = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {LogSearchRequest}
 */
export function decodeLogSearchRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeLogSearchRequestMessage(reader);
}



/**
 * @param {LogLine} message
 * @param {Writer} writer
 */
export function writeLogLine(message, writer) {
    if (message.time instanceof Date && message.time.getTime() !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int64(Math.trunc(message.time.getTime()));
    }
    if (message.level !== undefined && message.level !== null && message.level !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.level);
    }
    if (message.msg !== undefined && message.msg !== null && message.msg !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.msg);
    }
    if (message.props && Object.keys(message.props).length > 0) {
        for (const [rawKey, value] of Object.entries(message.props)) {
            const key = rawKey;
            writer.uint32(tag(4, WIRE.LDELIM)).fork();
            writer.uint32(tag(1, WIRE.LDELIM)).string(key);
            if (value !== undefined && value !== null && value !== "") {
                writer.uint32(tag(2, WIRE.LDELIM)).string(value);
            }
            writer.ldelim();
        }
    }
}


/**
 * @param {LogLine} message
 * @returns {Uint8Array}
 */
export function encodeLogLine(message) {
    const writer = Writer.create();
    writeLogLine(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {LogLine}
 */
function decodeLogLineMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {time: new Date(0), level: "", msg: "", props: {} };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.time = new Date(readInt64(reader, "int64"));
                break;
            }
            case 2: {
                message.level = reader.string();
                break;
            }
            case 3: {
                message.msg = reader.string();
                break;
            }
            case 4: {
                const end2 = reader.uint32() + reader.pos;
                let key = "";
                let value = "";
                while (reader.pos < end2) {
                    const tag2 = reader.uint32();
                    switch (tag2 >>> 3) {
                        case 1:
                            key = reader.string();
                            break;
                        case 2:
                            value = reader.string();
                            break;
                        default:
                            reader.skipType(tag2 & 7);
                    }
                }
                if (!message.props) { message.props = {}; }
                message.props[String(key)] = value;
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {LogLine}
 */
export function decodeLogLine(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeLogLineMessage(reader);
}



/**
 * @param {LogLineBatch} message
 * @param {Writer} writer
 */
export function writeLogLineBatch(message, writer) {
    if (message.lines && message.lines.length > 0) {
        for (const item of message.lines) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeLogLine(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {LogLineBatch} message
 * @returns {Uint8Array}
 */
export function encodeLogLineBatch(message) {
    const writer = Writer.create();
    writeLogLineBatch(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {LogLineBatch}
 */
function decodeLogLineBatchMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {lines: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.lines.push(decodeLogLineMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {LogLineBatch}
 */
export function decodeLogLineBatch(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeLogLineBatchMessage(reader);
}



/**
 * @param {DeploymentIdentifier} message
 * @param {Writer} writer
 */
export function writeDeploymentIdentifier(message, writer) {
    if (message.spaceId !== undefined && message.spaceId !== null && message.spaceId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.spaceId);
    }
    if (message.machine !== undefined && message.machine !== null && message.machine !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.machine);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {DeploymentIdentifier} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentIdentifier(message) {
    const writer = Writer.create();
    writeDeploymentIdentifier(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentIdentifier}
 */
function decodeDeploymentIdentifierMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {spaceId: 0, machine: "", name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.spaceId = reader.int32();
                break;
            }
            case 2: {
                message.machine = reader.string();
                break;
            }
            case 3: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentIdentifier}
 */
export function decodeDeploymentIdentifier(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentIdentifierMessage(reader);
}



/**
 * @param {DeploymentSpec} message
 * @param {Writer} writer
 */
export function writeDeploymentSpec(message, writer) {
    if (message.prepare !== undefined && message.prepare !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writePrepareConfig(message.prepare, writer);
        writer.ldelim();
    }
    if (message.runner !== undefined && message.runner !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeRunnerConfig(message.runner, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentSpec} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentSpec(message) {
    const writer = Writer.create();
    writeDeploymentSpec(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentSpec}
 */
function decodeDeploymentSpecMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {prepare: undefined, runner: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.prepare = decodePrepareConfigMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.runner = decodeRunnerConfigMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentSpec}
 */
export function decodeDeploymentSpec(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentSpecMessage(reader);
}



/**
 * @param {DesiredState} message
 * @param {Writer} writer
 */
export function writeDesiredState(message, writer) {
    if (message.version !== undefined && message.version !== null && message.version !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.version);
    }
    if (message.running === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.running);
    }
}


/**
 * @param {DesiredState} message
 * @returns {Uint8Array}
 */
export function encodeDesiredState(message) {
    const writer = Writer.create();
    writeDesiredState(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DesiredState}
 */
function decodeDesiredStateMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {version: "", running: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.version = reader.string();
                break;
            }
            case 2: {
                message.running = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DesiredState}
 */
export function decodeDesiredState(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDesiredStateMessage(reader);
}



/**
 * @param {DeploymentConfig} message
 * @param {Writer} writer
 */
export function writeDeploymentConfig(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(8, WIRE.VARINT)).int32(message.id);
    }
    if (message.configId !== undefined && message.configId !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentIdentifier(message.configId, writer);
        writer.ldelim();
    }
    if (message.version !== undefined && message.version !== null && message.version !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.version);
    }
    if (message.updatedAt instanceof Date && message.updatedAt.getTime() !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int64(Math.trunc(message.updatedAt.getTime()));
    }
    if (message.updatedBy !== undefined && message.updatedBy !== null && message.updatedBy !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.updatedBy);
    }
    if (message.spec !== undefined && message.spec !== null) {
        writer.uint32(tag(5, WIRE.LDELIM)).fork();
        writeDeploymentSpec(message.spec, writer);
        writer.ldelim();
    }
    if (message.desiredState !== undefined && message.desiredState !== null) {
        writer.uint32(tag(6, WIRE.LDELIM)).fork();
        writeDesiredState(message.desiredState, writer);
        writer.ldelim();
    }
    if (message.deleted === true) {
        writer.uint32(tag(7, WIRE.VARINT)).bool(message.deleted);
    }
    if (message.createdAt instanceof Date && message.createdAt.getTime() !== 0) {
        writer.uint32(tag(9, WIRE.VARINT)).int64(Math.trunc(message.createdAt.getTime()));
    }
}


/**
 * @param {DeploymentConfig} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentConfig(message) {
    const writer = Writer.create();
    writeDeploymentConfig(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentConfig}
 */
function decodeDeploymentConfigMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, configId: undefined, version: 0, updatedAt: new Date(0), updatedBy: 0, spec: undefined, desiredState: undefined, deleted: false, createdAt: new Date(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 8: {
                message.id = reader.int32();
                break;
            }
            case 1: {
                message.configId = decodeDeploymentIdentifierMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.version = reader.int32();
                break;
            }
            case 3: {
                message.updatedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 4: {
                message.updatedBy = reader.int32();
                break;
            }
            case 5: {
                message.spec = decodeDeploymentSpecMessage(reader, reader.uint32());
                break;
            }
            case 6: {
                message.desiredState = decodeDesiredStateMessage(reader, reader.uint32());
                break;
            }
            case 7: {
                message.deleted = reader.bool();
                break;
            }
            case 9: {
                message.createdAt = new Date(readInt64(reader, "int64"));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentConfig}
 */
export function decodeDeploymentConfig(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentConfigMessage(reader);
}



/**
 * @param {DeploymentWithStatus} message
 * @param {Writer} writer
 */
export function writeDeploymentWithStatus(message, writer) {
    if (message.config !== undefined && message.config !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentConfig(message.config, writer);
        writer.ldelim();
    }
    if (message.status !== undefined && message.status !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentStatus(message.status, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentWithStatus} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentWithStatus(message) {
    const writer = Writer.create();
    writeDeploymentWithStatus(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentWithStatus}
 */
function decodeDeploymentWithStatusMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {config: undefined, status: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.config = decodeDeploymentConfigMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.status = decodeDeploymentStatusMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentWithStatus}
 */
export function decodeDeploymentWithStatus(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentWithStatusMessage(reader);
}



/**
 * @param {DeploymentStatus} message
 * @param {Writer} writer
 */
export function writeDeploymentStatus(message, writer) {
    if (message.updatedAt !== undefined && message.updatedAt !== null) {
        writer.uint32(tag(7, WIRE.LDELIM)).fork();
        writeTimestamp(message.updatedAt, writer);
        writer.ldelim();
    }
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(6, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.preparer !== undefined && message.preparer !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writePreparerStatus(message.preparer, writer);
        writer.ldelim();
    }
    if (message.runner !== undefined && message.runner !== null) {
        writer.uint32(tag(5, WIRE.LDELIM)).fork();
        writeRunnerStatus(message.runner, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentStatus} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentStatus(message) {
    const writer = Writer.create();
    writeDeploymentStatus(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentStatus}
 */
function decodeDeploymentStatusMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {updatedAt: new Date(0), deploymentId: 0, preparer: undefined, runner: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 7: {
                message.updatedAt = decodeTimestampMessage(reader, reader.uint32());
                break;
            }
            case 6: {
                message.deploymentId = reader.int32();
                break;
            }
            case 4: {
                message.preparer = decodePreparerStatusMessage(reader, reader.uint32());
                break;
            }
            case 5: {
                message.runner = decodeRunnerStatusMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentStatus}
 */
export function decodeDeploymentStatus(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentStatusMessage(reader);
}



/**
 * @param {PreparerStatus} message
 * @param {Writer} writer
 */
export function writePreparerStatus(message, writer) {
    if (message.deploymentConfigVersion !== undefined && message.deploymentConfigVersion !== null && message.deploymentConfigVersion !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentConfigVersion);
    }
    if (message.artifact !== undefined && message.artifact !== null && message.artifact !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.artifact);
    }
    if (message.status !== undefined && message.status !== null && message.status !== 0) {
        writer.uint32(tag(3, WIRE.VARINT)).int32(message.status);
    }
}


/**
 * @param {PreparerStatus} message
 * @returns {Uint8Array}
 */
export function encodePreparerStatus(message) {
    const writer = Writer.create();
    writePreparerStatus(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {PreparerStatus}
 */
function decodePreparerStatusMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentConfigVersion: 0, artifact: "", status: 0 };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentConfigVersion = reader.int32();
                break;
            }
            case 2: {
                message.artifact = reader.string();
                break;
            }
            case 3: {
                message.status = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {PreparerStatus}
 */
export function decodePreparerStatus(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodePreparerStatusMessage(reader);
}



/**
 * @param {RunnerStatus} message
 * @param {Writer} writer
 */
export function writeRunnerStatus(message, writer) {
    if (message.deploymentConfigVersion !== undefined && message.deploymentConfigVersion !== null && message.deploymentConfigVersion !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentConfigVersion);
    }
    if (message.runningPid !== undefined && message.runningPid !== null && message.runningPid !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(message.runningPid);
    }
    if (message.runningArtifact !== undefined && message.runningArtifact !== null && message.runningArtifact !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.runningArtifact);
    }
    if (message.status !== undefined && message.status !== null && message.status !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int32(message.status);
    }
    if (message.numberOfRestarts !== undefined && message.numberOfRestarts !== null && message.numberOfRestarts !== 0) {
        writer.uint32(tag(6, WIRE.VARINT)).int32(message.numberOfRestarts);
    }
    if (message.lastRestartAt instanceof Date && message.lastRestartAt.getTime() !== 0) {
        writer.uint32(tag(7, WIRE.VARINT)).int64(Math.trunc(message.lastRestartAt.getTime()));
    }
    if (message.runningVersion !== undefined && message.runningVersion !== null && message.runningVersion !== "") {
        writer.uint32(tag(8, WIRE.LDELIM)).string(message.runningVersion);
    }
}


/**
 * @param {RunnerStatus} message
 * @returns {Uint8Array}
 */
export function encodeRunnerStatus(message) {
    const writer = Writer.create();
    writeRunnerStatus(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {RunnerStatus}
 */
function decodeRunnerStatusMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentConfigVersion: 0, runningPid: 0, runningArtifact: "", status: 0, numberOfRestarts: 0, lastRestartAt: new Date(0), runningVersion: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentConfigVersion = reader.int32();
                break;
            }
            case 2: {
                message.runningPid = reader.int32();
                break;
            }
            case 3: {
                message.runningArtifact = reader.string();
                break;
            }
            case 4: {
                message.status = reader.int32();
                break;
            }
            case 6: {
                message.numberOfRestarts = reader.int32();
                break;
            }
            case 7: {
                message.lastRestartAt = new Date(readInt64(reader, "int64"));
                break;
            }
            case 8: {
                message.runningVersion = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {RunnerStatus}
 */
export function decodeRunnerStatus(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeRunnerStatusMessage(reader);
}



/**
 * @param {Version} message
 * @param {Writer} writer
 */
export function writeVersion(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.id);
    }
    if (message.label !== undefined && message.label !== null && message.label !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.label);
    }
    if (message.author !== undefined && message.author !== null && message.author !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.author);
    }
    if (message.time instanceof Date && message.time.getTime() !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int64(Math.trunc(message.time.getTime()));
    }
}


/**
 * @param {Version} message
 * @returns {Uint8Array}
 */
export function encodeVersion(message) {
    const writer = Writer.create();
    writeVersion(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {Version}
 */
function decodeVersionMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: "", label: "", author: "", time: new Date(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.string();
                break;
            }
            case 2: {
                message.label = reader.string();
                break;
            }
            case 3: {
                message.author = reader.string();
                break;
            }
            case 4: {
                message.time = new Date(readInt64(reader, "int64"));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {Version}
 */
export function decodeVersion(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeVersionMessage(reader);
}



/**
 * @param {DeploymentVersions} message
 * @param {Writer} writer
 */
export function writeDeploymentVersions(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.nixDockerBuild !== undefined && message.nixDockerBuild !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentNixDockerBuildVersions(message.nixDockerBuild, writer);
        writer.ldelim();
    }
    if (message.githubRelease !== undefined && message.githubRelease !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writeDeploymentGithubReleaseVersions(message.githubRelease, writer);
        writer.ldelim();
    }
    if (message.containerImage !== undefined && message.containerImage !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeDeploymentContainerImageVersions(message.containerImage, writer);
        writer.ldelim();
    }
}


/**
 * @param {DeploymentVersions} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentVersions(message) {
    const writer = Writer.create();
    writeDeploymentVersions(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentVersions}
 */
function decodeDeploymentVersionsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, nixDockerBuild: undefined, githubRelease: undefined, containerImage: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.nixDockerBuild = decodeDeploymentNixDockerBuildVersionsMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.githubRelease = decodeDeploymentGithubReleaseVersionsMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.containerImage = decodeDeploymentContainerImageVersionsMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentVersions}
 */
export function decodeDeploymentVersions(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentVersionsMessage(reader);
}



/**
 * @param {DeploymentNixDockerBuildVersions} message
 * @param {Writer} writer
 */
export function writeDeploymentNixDockerBuildVersions(message, writer) {
    if (message.branches && message.branches.length > 0) {
        for (const item of message.branches) {
            writer.uint32(tag(1, WIRE.LDELIM)).string(item);
        }
    }
    if (message.selectedBranch !== undefined && message.selectedBranch !== null && message.selectedBranch !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.selectedBranch);
    }
    if (message.commits && message.commits.length > 0) {
        for (const item of message.commits) {
            writer.uint32(tag(3, WIRE.LDELIM)).fork();
            writeVersion(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {DeploymentNixDockerBuildVersions} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentNixDockerBuildVersions(message) {
    const writer = Writer.create();
    writeDeploymentNixDockerBuildVersions(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentNixDockerBuildVersions}
 */
function decodeDeploymentNixDockerBuildVersionsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {branches: [], selectedBranch: "", commits: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.branches.push(reader.string());
                break;
            }
            case 2: {
                message.selectedBranch = reader.string();
                break;
            }
            case 3: {
                message.commits.push(decodeVersionMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentNixDockerBuildVersions}
 */
export function decodeDeploymentNixDockerBuildVersions(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentNixDockerBuildVersionsMessage(reader);
}



/**
 * @param {DeploymentGithubReleaseVersions} message
 * @param {Writer} writer
 */
export function writeDeploymentGithubReleaseVersions(message, writer) {
    if (message.releases && message.releases.length > 0) {
        for (const item of message.releases) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeVersion(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {DeploymentGithubReleaseVersions} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentGithubReleaseVersions(message) {
    const writer = Writer.create();
    writeDeploymentGithubReleaseVersions(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentGithubReleaseVersions}
 */
function decodeDeploymentGithubReleaseVersionsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {releases: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.releases.push(decodeVersionMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentGithubReleaseVersions}
 */
export function decodeDeploymentGithubReleaseVersions(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentGithubReleaseVersionsMessage(reader);
}



/**
 * @param {DeploymentContainerImageVersions} message
 * @param {Writer} writer
 */
export function writeDeploymentContainerImageVersions(message, writer) {
    if (message.tags && message.tags.length > 0) {
        for (const item of message.tags) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeVersion(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {DeploymentContainerImageVersions} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentContainerImageVersions(message) {
    const writer = Writer.create();
    writeDeploymentContainerImageVersions(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentContainerImageVersions}
 */
function decodeDeploymentContainerImageVersionsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {tags: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.tags.push(decodeVersionMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentContainerImageVersions}
 */
export function decodeDeploymentContainerImageVersions(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentContainerImageVersionsMessage(reader);
}



/**
 * @param {DeploymentVersionsRequest} message
 * @param {Writer} writer
 */
export function writeDeploymentVersionsRequest(message, writer) {
    if (message.deploymentId !== undefined && message.deploymentId !== null && message.deploymentId !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.deploymentId);
    }
    if (message.selectedBranch !== undefined && message.selectedBranch !== null && message.selectedBranch !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.selectedBranch);
    }
}


/**
 * @param {DeploymentVersionsRequest} message
 * @returns {Uint8Array}
 */
export function encodeDeploymentVersionsRequest(message) {
    const writer = Writer.create();
    writeDeploymentVersionsRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {DeploymentVersionsRequest}
 */
function decodeDeploymentVersionsRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentId: 0, selectedBranch: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentId = reader.int32();
                break;
            }
            case 2: {
                message.selectedBranch = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {DeploymentVersionsRequest}
 */
export function decodeDeploymentVersionsRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeDeploymentVersionsRequestMessage(reader);
}



/**
 * @param {ValidateSourceRequest} message
 * @param {Writer} writer
 */
export function writeValidateSourceRequest(message, writer) {
    if (message.nixDockerBuild !== undefined && message.nixDockerBuild !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeValidateNixDockerBuildSource(message.nixDockerBuild, writer);
        writer.ldelim();
    }
    if (message.containerImage !== undefined && message.containerImage !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeValidateContainerImageSource(message.containerImage, writer);
        writer.ldelim();
    }
}


/**
 * @param {ValidateSourceRequest} message
 * @returns {Uint8Array}
 */
export function encodeValidateSourceRequest(message) {
    const writer = Writer.create();
    writeValidateSourceRequest(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateSourceRequest}
 */
function decodeValidateSourceRequestMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {nixDockerBuild: undefined, containerImage: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 2: {
                message.nixDockerBuild = decodeValidateNixDockerBuildSourceMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.containerImage = decodeValidateContainerImageSourceMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateSourceRequest}
 */
export function decodeValidateSourceRequest(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateSourceRequestMessage(reader);
}



/**
 * @param {ValidateNixDockerBuildSource} message
 * @param {Writer} writer
 */
export function writeValidateNixDockerBuildSource(message, writer) {
    if (message.repoUrl !== undefined && message.repoUrl !== null && message.repoUrl !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.repoUrl);
    }
    if (message.selectedBranch !== undefined && message.selectedBranch !== null && message.selectedBranch !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.selectedBranch);
    }
    if (message.selectedCommit !== undefined && message.selectedCommit !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writeVersion(message.selectedCommit, writer);
        writer.ldelim();
    }
    if (message.selectedFlakePath !== undefined && message.selectedFlakePath !== null && message.selectedFlakePath !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.selectedFlakePath);
    }
    if (message.refreshAvailableBranches === true) {
        writer.uint32(tag(5, WIRE.VARINT)).bool(message.refreshAvailableBranches);
    }
    if (message.refreshAvailableCommits === true) {
        writer.uint32(tag(6, WIRE.VARINT)).bool(message.refreshAvailableCommits);
    }
    if (message.checkRepo === true) {
        writer.uint32(tag(7, WIRE.VARINT)).bool(message.checkRepo);
    }
    if (message.checkBranch === true) {
        writer.uint32(tag(8, WIRE.VARINT)).bool(message.checkBranch);
    }
    if (message.checkCommit === true) {
        writer.uint32(tag(9, WIRE.VARINT)).bool(message.checkCommit);
    }
    if (message.checkFlakePath === true) {
        writer.uint32(tag(10, WIRE.VARINT)).bool(message.checkFlakePath);
    }
}


/**
 * @param {ValidateNixDockerBuildSource} message
 * @returns {Uint8Array}
 */
export function encodeValidateNixDockerBuildSource(message) {
    const writer = Writer.create();
    writeValidateNixDockerBuildSource(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateNixDockerBuildSource}
 */
function decodeValidateNixDockerBuildSourceMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {repoUrl: "", selectedBranch: "", selectedCommit: undefined, selectedFlakePath: "", refreshAvailableBranches: false, refreshAvailableCommits: false, checkRepo: false, checkBranch: false, checkCommit: false, checkFlakePath: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.repoUrl = reader.string();
                break;
            }
            case 2: {
                message.selectedBranch = reader.string();
                break;
            }
            case 3: {
                message.selectedCommit = decodeVersionMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.selectedFlakePath = reader.string();
                break;
            }
            case 5: {
                message.refreshAvailableBranches = reader.bool();
                break;
            }
            case 6: {
                message.refreshAvailableCommits = reader.bool();
                break;
            }
            case 7: {
                message.checkRepo = reader.bool();
                break;
            }
            case 8: {
                message.checkBranch = reader.bool();
                break;
            }
            case 9: {
                message.checkCommit = reader.bool();
                break;
            }
            case 10: {
                message.checkFlakePath = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateNixDockerBuildSource}
 */
export function decodeValidateNixDockerBuildSource(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateNixDockerBuildSourceMessage(reader);
}



/**
 * @param {ValidateNixDockerBuildSourceResponse} message
 * @param {Writer} writer
 */
export function writeValidateNixDockerBuildSourceResponse(message, writer) {
    if (message.checkedRepoUrl !== undefined && message.checkedRepoUrl !== null && message.checkedRepoUrl !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.checkedRepoUrl);
    }
    if (message.gitRepository !== undefined && message.gitRepository !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeValidationResult(message.gitRepository, writer);
        writer.ldelim();
    }
    if (message.checkedBranch !== undefined && message.checkedBranch !== null && message.checkedBranch !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.checkedBranch);
    }
    if (message.branchCheck !== undefined && message.branchCheck !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeValidationResult(message.branchCheck, writer);
        writer.ldelim();
    }
    if (message.checkedCommit !== undefined && message.checkedCommit !== null) {
        writer.uint32(tag(5, WIRE.LDELIM)).fork();
        writeVersion(message.checkedCommit, writer);
        writer.ldelim();
    }
    if (message.commitCheck !== undefined && message.commitCheck !== null) {
        writer.uint32(tag(6, WIRE.LDELIM)).fork();
        writeValidationResult(message.commitCheck, writer);
        writer.ldelim();
    }
    if (message.checkedFlakePath !== undefined && message.checkedFlakePath !== null && message.checkedFlakePath !== "") {
        writer.uint32(tag(7, WIRE.LDELIM)).string(message.checkedFlakePath);
    }
    if (message.nixFlakeFile !== undefined && message.nixFlakeFile !== null) {
        writer.uint32(tag(8, WIRE.LDELIM)).fork();
        writeValidationResult(message.nixFlakeFile, writer);
        writer.ldelim();
    }
    if (message.availableBranches !== undefined && message.availableBranches !== null) {
        writer.uint32(tag(9, WIRE.LDELIM)).fork();
        writeAvailableBranches(message.availableBranches, writer);
        writer.ldelim();
    }
    if (message.availableCommits !== undefined && message.availableCommits !== null) {
        writer.uint32(tag(10, WIRE.LDELIM)).fork();
        writeAvailableCommits(message.availableCommits, writer);
        writer.ldelim();
    }
}


/**
 * @param {ValidateNixDockerBuildSourceResponse} message
 * @returns {Uint8Array}
 */
export function encodeValidateNixDockerBuildSourceResponse(message) {
    const writer = Writer.create();
    writeValidateNixDockerBuildSourceResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateNixDockerBuildSourceResponse}
 */
function decodeValidateNixDockerBuildSourceResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {checkedRepoUrl: "", gitRepository: undefined, checkedBranch: "", branchCheck: undefined, checkedCommit: undefined, commitCheck: undefined, checkedFlakePath: "", nixFlakeFile: undefined, availableBranches: undefined, availableCommits: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.checkedRepoUrl = reader.string();
                break;
            }
            case 2: {
                message.gitRepository = decodeValidationResultMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.checkedBranch = reader.string();
                break;
            }
            case 4: {
                message.branchCheck = decodeValidationResultMessage(reader, reader.uint32());
                break;
            }
            case 5: {
                message.checkedCommit = decodeVersionMessage(reader, reader.uint32());
                break;
            }
            case 6: {
                message.commitCheck = decodeValidationResultMessage(reader, reader.uint32());
                break;
            }
            case 7: {
                message.checkedFlakePath = reader.string();
                break;
            }
            case 8: {
                message.nixFlakeFile = decodeValidationResultMessage(reader, reader.uint32());
                break;
            }
            case 9: {
                message.availableBranches = decodeAvailableBranchesMessage(reader, reader.uint32());
                break;
            }
            case 10: {
                message.availableCommits = decodeAvailableCommitsMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateNixDockerBuildSourceResponse}
 */
export function decodeValidateNixDockerBuildSourceResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateNixDockerBuildSourceResponseMessage(reader);
}



/**
 * @param {AvailableCommits} message
 * @param {Writer} writer
 */
export function writeAvailableCommits(message, writer) {
    if (message.loaded === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.loaded);
    }
    if (message.branch !== undefined && message.branch !== null && message.branch !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.branch);
    }
    if (message.errormessage !== undefined && message.errormessage !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.errormessage);
    }
    if (message.commits && message.commits.length > 0) {
        for (const item of message.commits) {
            writer.uint32(tag(4, WIRE.LDELIM)).fork();
            writeVersion(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {AvailableCommits} message
 * @returns {Uint8Array}
 */
export function encodeAvailableCommits(message) {
    const writer = Writer.create();
    writeAvailableCommits(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AvailableCommits}
 */
function decodeAvailableCommitsMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {loaded: false, branch: "", errormessage: undefined, commits: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.loaded = reader.bool();
                break;
            }
            case 2: {
                message.branch = reader.string();
                break;
            }
            case 3: {
                message.errormessage = reader.string();
                break;
            }
            case 4: {
                message.commits.push(decodeVersionMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AvailableCommits}
 */
export function decodeAvailableCommits(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAvailableCommitsMessage(reader);
}



/**
 * @param {AvailableBranches} message
 * @param {Writer} writer
 */
export function writeAvailableBranches(message, writer) {
    if (message.loaded === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.loaded);
    }
    if (message.errormessage !== undefined && message.errormessage !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.errormessage);
    }
    if (message.branches && message.branches.length > 0) {
        for (const item of message.branches) {
            writer.uint32(tag(3, WIRE.LDELIM)).string(item);
        }
    }
}


/**
 * @param {AvailableBranches} message
 * @returns {Uint8Array}
 */
export function encodeAvailableBranches(message) {
    const writer = Writer.create();
    writeAvailableBranches(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AvailableBranches}
 */
function decodeAvailableBranchesMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {loaded: false, errormessage: undefined, branches: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.loaded = reader.bool();
                break;
            }
            case 2: {
                message.errormessage = reader.string();
                break;
            }
            case 3: {
                message.branches.push(reader.string());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AvailableBranches}
 */
export function decodeAvailableBranches(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAvailableBranchesMessage(reader);
}



/**
 * @param {RepoValidationResult} message
 * @param {Writer} writer
 */
export function writeRepoValidationResult(message, writer) {
    if (message.checked === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.checked);
    }
    if (message.repoUrl !== undefined && message.repoUrl !== null && message.repoUrl !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.repoUrl);
    }
    if (message.ok === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.ok);
    }
    if (message.message !== undefined && message.message !== null && message.message !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.message);
    }
}


/**
 * @param {RepoValidationResult} message
 * @returns {Uint8Array}
 */
export function encodeRepoValidationResult(message) {
    const writer = Writer.create();
    writeRepoValidationResult(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {RepoValidationResult}
 */
function decodeRepoValidationResultMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {checked: false, repoUrl: "", ok: false, message: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.checked = reader.bool();
                break;
            }
            case 2: {
                message.repoUrl = reader.string();
                break;
            }
            case 3: {
                message.ok = reader.bool();
                break;
            }
            case 4: {
                message.message = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {RepoValidationResult}
 */
export function decodeRepoValidationResult(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeRepoValidationResultMessage(reader);
}



/**
 * @param {ValidationResult} message
 * @param {Writer} writer
 */
export function writeValidationResult(message, writer) {
    if (message.checked === true) {
        writer.uint32(tag(1, WIRE.VARINT)).bool(message.checked);
    }
    if (message.ok === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.ok);
    }
    if (message.message !== undefined && message.message !== null && message.message !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.message);
    }
}


/**
 * @param {ValidationResult} message
 * @returns {Uint8Array}
 */
export function encodeValidationResult(message) {
    const writer = Writer.create();
    writeValidationResult(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidationResult}
 */
function decodeValidationResultMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {checked: false, ok: false, message: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.checked = reader.bool();
                break;
            }
            case 2: {
                message.ok = reader.bool();
                break;
            }
            case 3: {
                message.message = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidationResult}
 */
export function decodeValidationResult(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidationResultMessage(reader);
}



/**
 * @param {ValidateContainerImageSource} message
 * @param {Writer} writer
 */
export function writeValidateContainerImageSource(message, writer) {
    if (message.image !== undefined && message.image !== null && message.image !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.image);
    }
    if (message.refreshVersions === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.refreshVersions);
    }
}


/**
 * @param {ValidateContainerImageSource} message
 * @returns {Uint8Array}
 */
export function encodeValidateContainerImageSource(message) {
    const writer = Writer.create();
    writeValidateContainerImageSource(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateContainerImageSource}
 */
function decodeValidateContainerImageSourceMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {image: "", refreshVersions: false };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.image = reader.string();
                break;
            }
            case 2: {
                message.refreshVersions = reader.bool();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateContainerImageSource}
 */
export function decodeValidateContainerImageSource(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateContainerImageSourceMessage(reader);
}



/**
 * @param {ValidateSourceResponse} message
 * @param {Writer} writer
 */
export function writeValidateSourceResponse(message, writer) {
    if (message.nixDockerBuild !== undefined && message.nixDockerBuild !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeValidateNixDockerBuildSourceResponse(message.nixDockerBuild, writer);
        writer.ldelim();
    }
    if (message.containerImage !== undefined && message.containerImage !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeValidateContainerImageSourceResponse(message.containerImage, writer);
        writer.ldelim();
    }
}


/**
 * @param {ValidateSourceResponse} message
 * @returns {Uint8Array}
 */
export function encodeValidateSourceResponse(message) {
    const writer = Writer.create();
    writeValidateSourceResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateSourceResponse}
 */
function decodeValidateSourceResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {nixDockerBuild: undefined, containerImage: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 2: {
                message.nixDockerBuild = decodeValidateNixDockerBuildSourceResponseMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.containerImage = decodeValidateContainerImageSourceResponseMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateSourceResponse}
 */
export function decodeValidateSourceResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateSourceResponseMessage(reader);
}



/**
 * @param {ValidateContainerImageSourceResponse} message
 * @param {Writer} writer
 */
export function writeValidateContainerImageSourceResponse(message, writer) {
    if (message.image !== undefined && message.image !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeValidationResult(message.image, writer);
        writer.ldelim();
    }
    if (message.tags && message.tags.length > 0) {
        for (const item of message.tags) {
            writer.uint32(tag(2, WIRE.LDELIM)).fork();
            writeVersion(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ValidateContainerImageSourceResponse} message
 * @returns {Uint8Array}
 */
export function encodeValidateContainerImageSourceResponse(message) {
    const writer = Writer.create();
    writeValidateContainerImageSourceResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ValidateContainerImageSourceResponse}
 */
function decodeValidateContainerImageSourceResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {image: undefined, tags: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.image = decodeValidationResultMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.tags.push(decodeVersionMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ValidateContainerImageSourceResponse}
 */
export function decodeValidateContainerImageSourceResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeValidateContainerImageSourceResponseMessage(reader);
}



/**
 * @param {User} message
 * @param {Writer} writer
 */
export function writeUser(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.name);
    }
}


/**
 * @param {User} message
 * @returns {Uint8Array}
 */
export function encodeUser(message) {
    const writer = Writer.create();
    writeUser(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {User}
 */
function decodeUserMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, name: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.name = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {User}
 */
export function decodeUser(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeUserMessage(reader);
}



/**
 * @param {WebAuthnCredential} message
 * @param {Writer} writer
 */
export function writeWebAuthnCredential(message, writer) {
    if (message.id && message.id.length > 0) {
        writer.uint32(tag(1, WIRE.LDELIM)).bytes(message.id);
    }
    if (message.data && message.data.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.data);
    }
}


/**
 * @param {WebAuthnCredential} message
 * @returns {Uint8Array}
 */
export function encodeWebAuthnCredential(message) {
    const writer = Writer.create();
    writeWebAuthnCredential(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {WebAuthnCredential}
 */
function decodeWebAuthnCredentialMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: new Uint8Array(0), data: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.bytes();
                break;
            }
            case 2: {
                message.data = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {WebAuthnCredential}
 */
export function decodeWebAuthnCredential(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeWebAuthnCredentialMessage(reader);
}



/**
 * @param {InternalUser} message
 * @param {Writer} writer
 */
export function writeInternalUser(message, writer) {
    if (message.id !== undefined && message.id !== null && message.id !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.id);
    }
    if (message.webAuthNId && message.webAuthNId.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.webAuthNId);
    }
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.name);
    }
    if (message.credentials && message.credentials.length > 0) {
        for (const item of message.credentials) {
            writer.uint32(tag(4, WIRE.LDELIM)).fork();
            writeWebAuthnCredential(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {InternalUser} message
 * @returns {Uint8Array}
 */
export function encodeInternalUser(message) {
    const writer = Writer.create();
    writeInternalUser(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {InternalUser}
 */
function decodeInternalUserMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {id: 0, webAuthNId: new Uint8Array(0), name: "", credentials: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.id = reader.int32();
                break;
            }
            case 2: {
                message.webAuthNId = reader.bytes();
                break;
            }
            case 3: {
                message.name = reader.string();
                break;
            }
            case 4: {
                message.credentials.push(decodeWebAuthnCredentialMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {InternalUser}
 */
export function decodeInternalUser(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeInternalUserMessage(reader);
}



/**
 * @param {PublicKeyRecord} message
 * @param {Writer} writer
 */
export function writePublicKeyRecord(message, writer) {
    if (message.kid !== undefined && message.kid !== null && message.kid !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.kid);
    }
    if (message.keyBytes && message.keyBytes.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.keyBytes);
    }
}


/**
 * @param {PublicKeyRecord} message
 * @returns {Uint8Array}
 */
export function encodePublicKeyRecord(message) {
    const writer = Writer.create();
    writePublicKeyRecord(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {PublicKeyRecord}
 */
function decodePublicKeyRecordMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {kid: "", keyBytes: new Uint8Array(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.kid = reader.string();
                break;
            }
            case 2: {
                message.keyBytes = reader.bytes();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {PublicKeyRecord}
 */
export function decodePublicKeyRecord(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodePublicKeyRecordMessage(reader);
}



/**
 * @param {ClusterMachine} message
 * @param {Writer} writer
 */
export function writeClusterMachine(message, writer) {
    if (message.name !== undefined && message.name !== null && message.name !== "") {
        writer.uint32(tag(1, WIRE.LDELIM)).string(message.name);
    }
    if (message.isPrimary === true) {
        writer.uint32(tag(2, WIRE.VARINT)).bool(message.isPrimary);
    }
    if (message.connected === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.connected);
    }
    if (message.connectedAt instanceof Date && message.connectedAt.getTime() !== 0) {
        writer.uint32(tag(4, WIRE.VARINT)).int64(Math.trunc(message.connectedAt.getTime()));
    }
}


/**
 * @param {ClusterMachine} message
 * @returns {Uint8Array}
 */
export function encodeClusterMachine(message) {
    const writer = Writer.create();
    writeClusterMachine(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterMachine}
 */
function decodeClusterMachineMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {name: "", isPrimary: false, connected: false, connectedAt: new Date(0) };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.name = reader.string();
                break;
            }
            case 2: {
                message.isPrimary = reader.bool();
                break;
            }
            case 3: {
                message.connected = reader.bool();
                break;
            }
            case 4: {
                message.connectedAt = new Date(readInt64(reader, "int64"));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterMachine}
 */
export function decodeClusterMachine(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterMachineMessage(reader);
}



/**
 * @param {ClusterStatusResponse} message
 * @param {Writer} writer
 */
export function writeClusterStatusResponse(message, writer) {
    if (message.machines && message.machines.length > 0) {
        for (const item of message.machines) {
            writer.uint32(tag(1, WIRE.LDELIM)).fork();
            writeClusterMachine(item, writer);
            writer.ldelim();
        }
    }
}


/**
 * @param {ClusterStatusResponse} message
 * @returns {Uint8Array}
 */
export function encodeClusterStatusResponse(message) {
    const writer = Writer.create();
    writeClusterStatusResponse(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ClusterStatusResponse}
 */
function decodeClusterStatusResponseMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {machines: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.machines.push(decodeClusterMachineMessage(reader, reader.uint32()));
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ClusterStatusResponse}
 */
export function decodeClusterStatusResponse(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeClusterStatusResponseMessage(reader);
}



/**
 * @param {MsgToWorker} message
 * @param {Writer} writer
 */
export function writeMsgToWorker(message, writer) {
    if (message.deploymentsSnapshot !== undefined && message.deploymentsSnapshot !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentWithStatusSnapshot(message.deploymentsSnapshot, writer);
        writer.ldelim();
    }
    if (message.deploymentUpdate !== undefined && message.deploymentUpdate !== null) {
        writer.uint32(tag(2, WIRE.LDELIM)).fork();
        writeDeploymentConfig(message.deploymentUpdate, writer);
        writer.ldelim();
    }
    if (message.prepareLogRequest !== undefined && message.prepareLogRequest !== null) {
        writer.uint32(tag(3, WIRE.LDELIM)).fork();
        writePrepareOutputRequest(message.prepareLogRequest, writer);
        writer.ldelim();
    }
    if (message.runLogRequest !== undefined && message.runLogRequest !== null) {
        writer.uint32(tag(4, WIRE.LDELIM)).fork();
        writeRunOutputRequest(message.runLogRequest, writer);
        writer.ldelim();
    }
    if (message.deploymentLogRequest !== undefined && message.deploymentLogRequest !== null) {
        writer.uint32(tag(5, WIRE.LDELIM)).fork();
        writeDeploymentLogRequest(message.deploymentLogRequest, writer);
        writer.ldelim();
    }
    if (message.stopLogRequestId !== undefined && message.stopLogRequestId !== null && message.stopLogRequestId !== "") {
        writer.uint32(tag(6, WIRE.LDELIM)).string(message.stopLogRequestId);
    }
    if (message.logSearchRequest !== undefined && message.logSearchRequest !== null) {
        writer.uint32(tag(7, WIRE.LDELIM)).fork();
        writeLogSearchRequest(message.logSearchRequest, writer);
        writer.ldelim();
    }
}


/**
 * @param {MsgToWorker} message
 * @returns {Uint8Array}
 */
export function encodeMsgToWorker(message) {
    const writer = Writer.create();
    writeMsgToWorker(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {MsgToWorker}
 */
function decodeMsgToWorkerMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {deploymentsSnapshot: undefined, deploymentUpdate: undefined, prepareLogRequest: undefined, runLogRequest: undefined, deploymentLogRequest: undefined, stopLogRequestId: "", logSearchRequest: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.deploymentsSnapshot = decodeDeploymentWithStatusSnapshotMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.deploymentUpdate = decodeDeploymentConfigMessage(reader, reader.uint32());
                break;
            }
            case 3: {
                message.prepareLogRequest = decodePrepareOutputRequestMessage(reader, reader.uint32());
                break;
            }
            case 4: {
                message.runLogRequest = decodeRunOutputRequestMessage(reader, reader.uint32());
                break;
            }
            case 5: {
                message.deploymentLogRequest = decodeDeploymentLogRequestMessage(reader, reader.uint32());
                break;
            }
            case 6: {
                message.stopLogRequestId = reader.string();
                break;
            }
            case 7: {
                message.logSearchRequest = decodeLogSearchRequestMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {MsgToWorker}
 */
export function decodeMsgToWorker(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeMsgToWorkerMessage(reader);
}



/**
 * @param {MsgToMaster} message
 * @param {Writer} writer
 */
export function writeMsgToMaster(message, writer) {
    if (message.statusWrite !== undefined && message.statusWrite !== null) {
        writer.uint32(tag(1, WIRE.LDELIM)).fork();
        writeDeploymentStatus(message.statusWrite, writer);
        writer.ldelim();
    }
    if (message.logData && message.logData.length > 0) {
        writer.uint32(tag(2, WIRE.LDELIM)).bytes(message.logData);
    }
    if (message.logEnd === true) {
        writer.uint32(tag(3, WIRE.VARINT)).bool(message.logEnd);
    }
    if (message.logRequestId !== undefined && message.logRequestId !== null && message.logRequestId !== "") {
        writer.uint32(tag(4, WIRE.LDELIM)).string(message.logRequestId);
    }
    if (message.logLines !== undefined && message.logLines !== null) {
        writer.uint32(tag(5, WIRE.LDELIM)).fork();
        writeLogLineBatch(message.logLines, writer);
        writer.ldelim();
    }
}


/**
 * @param {MsgToMaster} message
 * @returns {Uint8Array}
 */
export function encodeMsgToMaster(message) {
    const writer = Writer.create();
    writeMsgToMaster(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {MsgToMaster}
 */
function decodeMsgToMasterMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {statusWrite: undefined, logData: new Uint8Array(0), logEnd: false, logRequestId: "", logLines: undefined };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.statusWrite = decodeDeploymentStatusMessage(reader, reader.uint32());
                break;
            }
            case 2: {
                message.logData = reader.bytes();
                break;
            }
            case 3: {
                message.logEnd = reader.bool();
                break;
            }
            case 4: {
                message.logRequestId = reader.string();
                break;
            }
            case 5: {
                message.logLines = decodeLogLineBatchMessage(reader, reader.uint32());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {MsgToMaster}
 */
export function decodeMsgToMaster(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeMsgToMasterMessage(reader);
}



/**
 * @param {AccessPolicy} message
 * @param {Writer} writer
 */
export function writeAccessPolicy(message, writer) {
    if (message.policyType !== undefined && message.policyType !== null && message.policyType !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.policyType);
    }
    if (message.scopes && message.scopes.length > 0) {
        for (const item of message.scopes) {
            writer.uint32(tag(2, WIRE.LDELIM)).string(item);
        }
    }
}


/**
 * @param {AccessPolicy} message
 * @returns {Uint8Array}
 */
export function encodeAccessPolicy(message) {
    const writer = Writer.create();
    writeAccessPolicy(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {AccessPolicy}
 */
function decodeAccessPolicyMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {policyType: 0, scopes: [] };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.policyType = reader.int32();
                break;
            }
            case 2: {
                message.scopes.push(reader.string());
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {AccessPolicy}
 */
export function decodeAccessPolicy(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeAccessPolicyMessage(reader);
}



/**
 * @param {ApiErr} message
 * @param {Writer} writer
 */
export function writeApiErr(message, writer) {
    if (message.code !== undefined && message.code !== null && message.code !== 0) {
        writer.uint32(tag(1, WIRE.VARINT)).int32(message.code);
    }
    if (message.displayErr !== undefined && message.displayErr !== null && message.displayErr !== "") {
        writer.uint32(tag(2, WIRE.LDELIM)).string(message.displayErr);
    }
    if (message.internalErr !== undefined && message.internalErr !== null && message.internalErr !== "") {
        writer.uint32(tag(3, WIRE.LDELIM)).string(message.internalErr);
    }
}


/**
 * @param {ApiErr} message
 * @returns {Uint8Array}
 */
export function encodeApiErr(message) {
    const writer = Writer.create();
    writeApiErr(message, writer);
    return writer.finish();
}


/**
 * @param {Reader} reader
 * @param {number} [length]
 * @returns {ApiErr}
 */
function decodeApiErrMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = {code: 0, displayErr: "", internalErr: "" };
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                message.code = reader.int32();
                break;
            }
            case 2: {
                message.displayErr = reader.string();
                break;
            }
            case 3: {
                message.internalErr = reader.string();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return message;
}


/**
 * @param {ArrayBuffer} buffer
 * @returns {ApiErr}
 */
export function decodeApiErr(buffer) {
    const reader = Reader.create(new Uint8Array(buffer));
    return decodeApiErrMessage(reader);
}



function readInt64(reader, method) {
    const value = reader[method]();
    if (typeof value === "number") {
        return value;
    }
    if (typeof value === "bigint") {
        return Number(value);
    }
    return value.toNumber();
}
function writeTimestamp(value, writer) {
    if (!(value instanceof Date)) {
        return;
    }
    const ms = value.getTime();
    const seconds = Math.floor(ms / 1000);
    const nanos = Math.floor((ms - (seconds * 1000)) * 1e6);
    writer.uint32(tag(1, WIRE.VARINT)).int64(seconds);
    if (nanos !== 0) {
        writer.uint32(tag(2, WIRE.VARINT)).int32(nanos);
    }
}

function decodeTimestampMessage(reader, length) {
    const end = length === undefined ? reader.len : reader.pos + length;
    let seconds = 0;
    let nanos = 0;
    while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
            case 1: {
                seconds = readInt64(reader, "int64");
                break;
            }
            case 2: {
                nanos = reader.int32();
                break;
            }
            default:
                reader.skipType(tag & 7);
        }
    }
    return new Date((seconds * 1000) + Math.floor(nanos / 1e6));
}
